/**
 * LEGACY DISCONNECTED.
 *
 * The old timber-specific workspace used `/api/timber/*`, which is now a
 * governed 410 Gone API. Keep this file only as a compatibility import shell:
 * any accidental import lands on the active barter/payment-in-goods workspace.
 */
export { default } from './BarterWorkspaceView';
