/**
 * @deprecated Legacy compatibility entry.
 * Canonical route should mount `pages/adjustment/AdjustmentCenterView` directly.
 * Keep as thin forwarder only; do not add page logic here.
 */
export { default } from './adjustment/AdjustmentCenterView';
