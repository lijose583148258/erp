# 浏览器 `spawn EPERM` 调研与执行方案（2026-04-16）

## 1. 现象
- 浏览器级脚本统一失败，报告为 `PLAYWRIGHT_SPAWN_EPERM`。
- API 级链路可通过，说明业务后端不等于故障根因。

## 2. 调研结论（基于官方资料）
- Node `EPERM` 属于操作系统权限/策略层错误，而非业务代码语义错误。  
  参考：[Node.js Errors](https://nodejs.org/api/errors.html)
- Playwright 可使用 bundled Chromium 或 `executablePath` 指向系统浏览器，但浏览器进程本身仍受系统策略控制。  
  参考：[Playwright BrowserType](https://playwright.dev/docs/api/class-browsertype)
- Windows 安全策略（如受控文件夹访问）会阻止进程创建/访问，需要显式放行应用。  
  参考：[Microsoft 支持 - 允许应用通过受控文件夹访问](https://support.microsoft.com/windows/allow-an-app-through-controlled-folder-access)

## 3. 执行策略（已接入脚本）
1. 先跑运行层探针，若是 `EPERM` 直接标记 `blocked_env`，不再误报“业务失败”。
2. 审计报告自动输出 remediation 建议与参考链接。
3. 保留 API 审计继续推进，避免浏览器层阻断拖死主线任务。

## 4. 建议放行清单（Windows）
- `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`
- `C:\Program Files\Google\Chrome\Application\chrome.exe`
- `%LOCALAPPDATA%\ms-playwright\**`（Playwright 浏览器目录）
- `node.exe`（执行脚本进程）

## 5. 验收标准
- `node scripts/browser-runtime-probe.cjs` 返回 `passed`
- 再执行 `node scripts/production-browser-audit-v1.cjs` 不再出现 `PLAYWRIGHT_SPAWN_EPERM`
- 页面审计报告状态从 `blocked_env` 变为 `passed`
