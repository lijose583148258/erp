# 阶段3测试数据保洁日检与冲突链收口_2026-04-23

## 本包目标

本包解决三个问题：

| 问题 | 处理 |
| --- | --- |
| 稳定库里审计/测试数据越来越多 | 新增只读 report-only 清单，不删除任何数据 |
| 日检是否能持续发现包状态问题 | 日检发现 package freshness 失败，重建后恢复 PASS |
| 并发和冲突链是否继续可控 | 补跑采购部分收货、发货签收、生产完工三条冲突链 |

## 代码和入口变更

| 文件 | 变更 |
| --- | --- |
| `scripts/test-data-retention-inventory-audit-v1.ts` | 新增稳定库测试/审计数据只读盘点脚本 |
| `package.json` | 新增 `audit:test-data:inventory` |

该脚本只读扫描 `D:\AilaoDaRuntime\stable.db`，输出 JSON/Markdown 报告，`destructiveActionsPerformed` 固定为 `0`。

## 测试数据保洁盘点结果

| 项目 | 结果 |
| --- | --- |
| 报告 | `F:\爱牢达\output\audit\test-data-retention-inventory-audit-v1.json` |
| Markdown | `F:\爱牢达\output\audit\test-data-retention-inventory-audit-v1.md` |
| 扫描表数 | 47 |
| 标记规则 | 8 |
| 列级命中组 | 76 |
| 涉及表 | 21 |
| P0 不可直接删除表 | 1 |
| P1 需备份和白名单表 | 13 |
| P2 仅人工复核表 | 7 |
| 实际删除 | 0 |

### 风险分层

| 风险 | 表 | 原因 |
| --- | --- | --- |
| P0 | `audit_logs` | 审计日志包含运行、备份、认证、BOM、UI smoke 等证据，不能直接删除 |
| P1 | `stock_movements`、`inventory_cost_ledgers`、`stock_balances`、`product_batches`、`production_bom_items`、`stock_entries`、`production_boms`、`production_work_orders` 等 | 涉及库存、成本、BOM、工单和批次链，必须先备份、建白名单、核对父子表和财务/库存影响 |
| P2 | `customers`、`shipments`、`orders`、`order_items`、`auth_roles`、`barter_*` 等 | 先人工复核来源，不做本轮清理 |

## 日检结果

| 阶段 | 结果 | 说明 |
| --- | --- | --- |
| 首次日检 | FAIL | 新增审计脚本和 `package.json` 后，root `dist` 新鲜度落后，被 `package-freshness` 正确拦截 |
| 修复动作 | `npm run build` | 刷新 root 构建产物，不改稳定包业务数据 |
| 复跑日检 | PASS | `audit:phase3:daily:package` 通过 |
| 当前 ledger | `totalRuns=15`，`passedRuns=14`，`failedRuns=1`，`consecutivePasses=2` | 失败已被记录，不伪装成未发生 |

## 包模式 soak

| 项目 | 结果 |
| --- | --- |
| 报告 | `F:\爱牢达\output\audit\phase3-local-soak-audit-20260423140045.json` |
| 总任务 | 7 |
| PASS | 7 |
| failed | 0 |
| stuck | 0 |
| 看板结论 | `ready-for-local-soak-with-watchlist` |
| watchlist | 历史影子 DB 文件 15 个，仍为 P2 观察项 |

## 冲突链补跑结果

| 链路 | 证据 | 结果 |
| --- | --- | --- |
| 采购部分收货并发 | `F:\爱牢达\output\playwright\procurement-partial-receipt-concurrency-audit-report-v1.json` | PASS，一个请求成功，一个请求 409 阻止超收，最终收货 10，成本台账 2 行 |
| 发货签收并发 | `F:\爱牢达\output\playwright\shipping-receipt-concurrency-audit-report-v1.json` | PASS，超签 409，异常差异生成，旧接口重复签收被阻止 |
| 生产完工并发 | `F:\爱牢达\output\playwright\production-completion-concurrency-audit-report-v1.json` | PASS，原料扣减 10 项，成品入库 1 项，成本台账 1 行 |

## 当前判断

| 判断 | 结论 |
| --- | --- |
| 是否可以直接清测试数据 | 不可以 |
| 是否有清理方向 | 有，先做白名单和依赖图，不碰 `audit_logs` 和库存/成本链 |
| 日检是否能发现问题 | 可以，本包中已真实拦截一次新鲜度问题 |
| 是否恢复稳定 | 已恢复，日检和 soak 均 PASS |
| 是否可宣称两个月稳定 | 不可以，还需要持续运行 ledger |

## 下一步

| 优先级 | 动作 |
| --- | --- |
| P0 | 做测试数据白名单导出，不删除，只把候选记录按业务链分组 |
| P1 | 对库存/成本/BOM/工单候选建立依赖图，避免清理造成库存和财务断链 |
| P1 | 对 UI 做重复点击、刷新中断、断网恢复这类极限交互测试 |
| P2 | 将历史影子 DB 文件继续保持隔离观察，不做删除 |

