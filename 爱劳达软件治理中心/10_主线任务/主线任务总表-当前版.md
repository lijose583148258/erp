# 主线任务总表（当前版）

## 当前六包状态
1. 包0：治理与统一收口 - 已完成
2. 包1：数据库底座 - 已完成
3. 包2：回款闭环 - 已完成
4. 包3：生产管理主链路 - 已完成轻量工作台闭环
5. 包4：财务报表与经营分析 - 已完成财务经营工作台与台账/现金流读模型
6. 包5：部署稳定性与上线验收 - 已完成
7. 扩展收口：收发货差异工作台与容差规则管理 - 已完成首包，浏览器级验收受当前环境 `spawn EPERM` 阻断，不能计为浏览器通过
8. 扩展收口：收发货差异处置动作桥与浏览器 CDP 兜底 - 已完成 API 闭环、客户差异转 RMA、防重复提交、CDP 探针修复，收发货差异工作台浏览器真实点测已通过；全系统逐页浏览器点测仍需继续
9. P0 浏览器验收底座：核心模块 CDP 逐页烟测 - 18 个核心路由已通过页面级烟测，截图和控制台错误证据已留存；业务写入闭环仍需按模块继续深测
10. CRM 客户主数据浏览器深测 - 已通过真实浏览器创建客户，API 读回三语名称、2 地址、2 联系人，浏览器刷新搜索可找回；客户池和角色权限深测仍需继续
11. 系统全盘梳理与风险台账 - 已完成目录/技术栈/启动链/路由链/数据库模型/大文件拆分/验收证据/风险分级梳理；当前 build、backend build、db audit、CDP 18 路由烟测通过，lint 为 0 error、109 warning
12. 备份恢复口径统一包 - 已修复备份目录随 `process.cwd()` 漂移的问题；CLI、API、后端运行时统一指向 `F:\爱牢达\backups`；备份创建、API 恢复、恢复后登录、DB audit、CDP 18 路由复核均通过
13. CRM 权限与 AI 隔离深测包 - 已修复 CRM 读取类接口只认证不授权的 P0 缺口；direct/channel/mixed、finance/warehouse 禁入、销售私海导出、AI 浏览器拒绝敏感客户名单请求均已通过；DB audit 与 CDP 18 路由复核通过
14. 结构目录与运行链梳理 - 已补充当前正式源码层、治理层、运行产物层、浏览器审计层、前端入口链、后端路由链、运行数据库、实际挂载页面和下一包权限修复顺序；后续修改必须先按该结构定位
15. 权限同类读入口收口包 - 已按结构链收口订单、合同、发货、售后、样品读接口角色门禁；新增角色读权限矩阵审计 46 步通过；build、lint、db audit、跨模块权限、CDP 18 路由烟测和乱码闸门均通过
16. 多智能体大闭环审计 - 已并行完成权限口径、API 大闭环、化工 BOM、并发对账、浏览器人工流审计；后端/API/并发/核心烟测通过，浏览器真实输入回读失败，不能计为全页面人工验收完成
17. 账号角色授权能力审计 - 已验证管理员创建账号、固定角色改派、角色访问边界和禁用登录；并修复角色改派后 segment 未归一的问题。26 步 API 审计通过。结论：固定角色授权链路可用，动态 RBAC 和数据范围权限仍需下一包治理
18. 权限三层模型与 Casbin 适配首包 - 已引入 Casbin，建立角色包/权限点/数据范围注册表，新增 `authorizePermission()`，迁移客户、订单、采购、仓储、团队、财务、看板主链路，前端菜单改为权限过滤；权限模型审计 54 步通过。动态角色创建与勾选权限仍需下一包
19. 动态角色权限成熟开源调研与首包闭环 - 已参考 Casbin、OpenFGA、NocoBase、Directus、Strapi 的成熟 RBAC 方向，补成动态角色表、权限表、角色权限表、角色 API、登录权限回传、前端按权限菜单过滤、角色禁用拦截登录/刷新/旧 token；动态角色验收脚本已通过
20. 主线防跑偏执行制度 - 已固定“每包开工前 6 问、六条主线、三阶段推进、完成证据、结束反思制度”；后续所有任务必须先对齐主线，不允许只因临时问题跳去新方向
21. 动态角色权限 UI 闭环 - 已在团队管理页新增独立角色权限管理面板，支持自定义角色创建、权限点搜索勾选、数据范围勾选、启停保存、团队成员选择自定义角色；浏览器真实操作和 API 回读均通过
22. 权限注册表统一包 - 已补齐前端菜单使用但后端动态角色未注册的模块读权限，新增前后端权限码同步审计；权限矩阵、动态角色、浏览器角色 UI 均已回归通过
23. AI 数据隔离复核 - 已确认 AI 上下文只保留计数与当前用户基础信息，外部 AI 默认关闭且敏感内容拦截；AI 安全回归与 CRM/AI 权限审计均通过
24. 客户数据范围与动态角色落地 - 已修复客户创建/编辑权限点错位，清理客户控制器内固定角色硬编码，让自定义 `own_customers/team_customers` 角色进入客户读取、统计、详情、创建、编辑真实链路；客户数据范围端到端审计、权限矩阵、动态角色、CRM/AI 隔离、前后端构建和 lint 均已通过
25. 订单回款数据范围落地 - 已修复订单创建/编辑权限点错位，抽出统一 `recordAccess` 记录级范围工具，让自定义 `own_customers/team_customers/finance_visible` 进入订单列表、统计、详情、导出、回款台账、回款摘要真实链路；订单回款范围审计、订单 API、客户范围、权限矩阵、动态角色、CRM/AI 隔离、前后端构建和 lint 均已通过
26. 采购仓储资产货抵数据范围落地 - 已补齐 `procurement_visible/warehouse_visible/finance_visible` 在采购、仓储、资产、货抵模块的真实数据范围；新增 `assets.write` 与 `barter.write/barter.approve/barter.post` 权限点；动态角色专项审计、权限矩阵、动态角色、客户范围、订单回款、采购、货抵、资产调账、前后端构建和 lint 均已通过
27. 客户运营链路动态权限与数据范围落地 - 已将合同、样品、售后、发货四条客户相关运营链路从固定角色白名单迁移到动态权限点；新增 `contracts.write`、`samples.create/status.manage`、`shipping.write/receipts.read`、`rma.write/resolve`；补齐客户范围、仓储范围、财务签收范围的记录级判断；客户运营专项审计、动态角色、客户范围、订单回款、运营范围、前后端构建和 lint 均已通过
28. 生产差异调整运营权限落地 - 已将生产、收发货差异、调整单中心从固定角色白名单迁移到动态权限点；新增 `production.write`、`discrepancies.write/rules.manage`、`adjustments.read/write/apply/reverse`；补齐生产仓储/财务范围、差异仓储/财务范围、调整单领域范围；运营专项审计、动态角色、客户运营、采购仓储资产货抵、前后端构建和 lint 均已通过
29. 回款动作权限与数据范围落地 - 已将回款中心动作接口从固定角色白名单迁移到动态权限点；新增 `collections.sync/reminder.write/promise.write/dispute.write/hold.manage`；补齐催收、承诺付款、争议、冻结、逾期同步、回款核验的数据范围和职责分离守卫；回款动作专项审计、动态角色、订单回款数据范围、前后端构建和 lint 均已通过
30. 回款中心前端权限与加载稳定 - 已修复 Vite 扫描运行产物导致的开发服务噪声、回款中心 StrictMode 双请求导致的首屏假空/超时、lint 扫描 `.vite-cache` 的假错误；新增浏览器级角色验收，viewer/operator/finance 按真实按钮显隐、承诺付款提交、核销回读全部通过；前端构建、lint、浏览器验收均已通过
31. 回款中心聚合读模型与性能闸门 - 已新增 `GET /api/collections/workbench` 聚合读接口，前端优先使用聚合接口并保留旧 7 接口回退；新增只读性能闸门脚本，实测聚合接口 3 轮全部通过、旧 7 接口并发仍接近 5 秒，浏览器级回款权限验收复核通过；前后端构建、lint、性能报告、浏览器验收均已通过
32. 启动链防误伤与智仔隔离 - 已移除稳定启动器对 `5173` 的清理，开发启动器不再全局 `taskkill node.exe`；复核当前 `3000/5001` 属于爱劳达、`5173/5180` 属于智仔；PowerShell 解析、运行时健康检查、乱码闸门均已通过
33. 全仓代码深度审计 - 已建立全仓审计脚本，修复登录页演示账号/角色假选择、固定角色路由、旧 mock 后端入口、启动脚本密码展示和金额地区格式漂移；当前全仓 P1 降至 2，均为客户 controller 与货抵 service 大文件拆分债；前后端构建、lint、运行健康、权限注册表、动态 RBAC、权限模型、跨模块权限和浏览器登录点测均已通过
34. 客户控制器拆分首包 - 已将客户主数据控制器中的类型、payload 归一化、权限数据范围、联系人地址持久化辅助拆出到 `backend/src/controllers/customer/`；客户 controller 降至 1027 行，全仓 P1 降至 1，剩余唯一 P1 为 `barter.service.ts`；前后端构建、lint、运行健康、客户数据范围、客户运营权限和 CRM 浏览器主数据审计均已通过
35. 货抵服务拆分与验收脚本收口 - 已将货抵类型、金额计算、状态断言、显示/metadata、库存凭证辅助拆出到 `backend/src/services/barter/`；全仓 P1 归零；修正货抵浏览器验收脚本的客户种子、选中协议判断、审核/过账响应等待和库存种子，旧 `barter-browser-audit-v2.cjs` 默认委托当前“协议 + 分批执行”验收；前后端构建、lint、运行健康、货抵权限 API、库存闭环和浏览器协议两批执行均已通过
36. 旧 Mock 入口审计口径收口 - 已确认 `backend/complete-server.js` 为强制退出的禁用旧 mock 入口，正式启动链不引用；全仓审计新增 `disabledLegacyFiles`，避免已禁用旧入口继续污染活跃大文件 P2；全仓审计与 lint 均已通过
37. 运行链误杀与超时收口 - 已为 `scripts/start-stable-v2.ps1` 增加进程归属守卫和同 PID 去重，5001 被非爱牢达占用时失败而不强杀，3000/3001/3002/4173 被外部进程占用时只警告跳过；`check-timeboxed-health.ps1` 增加 300 秒总时限并把日志收回 `logs/timeboxed-health`；当前 5001 为爱牢达，5173/5180 仍为智仔，runtime 健康、lint 和全仓审计均已复核通过
38. 启动入口统一与安全停止收口 - 已将 `scripts/stop-runtime.ps1` 改为只停止爱牢达归属进程，移除 5173 和全局 `node.exe` 停止；`启动系统.bat dev` 改走安全停止；`稳定启动.bat` 启动成功后打开 `http://127.0.0.1:5001/`；浏览器烟测、UI 截图和回款 UI 权限脚本默认入口统一到 5001；安全停止/恢复、runtime 健康、浏览器烟测、前端构建、lint、全仓审计均已通过
39. Lint 降噪首包 - 已按低风险原则清理未使用导入、变量、类型、本地函数和无用 catch 参数；lint 从 100 warnings 降到 15 warnings，保留 hook 依赖和 Fast Refresh 类结构警告不机械硬改；前端构建、运行健康、浏览器烟测和全仓审计均已通过，P1 仍为 0
40. 稳定口浏览器逐页回归与旧请求防踢登录 - 已修复 CDP 逐页脚本固定等待假红、API timeout 串页误判、旧请求 401 误踢登录、旧 smoke 文案等待不足和 shipping BOM 头导入 warning；5001 稳定口 18 路由浏览器回归 `passed_with_warnings`，无 HTTP 失败、无 pageerror、无 fatal console error；核心 smoke、前端构建、runtime 健康、全仓审计均已通过，lint 为 0 error、14 warning；剩余风险为快速切页下的 API timeout warning，需要下一包做请求取消/聚合读模型治理
41. 前端请求生命周期取消首包 - 已为 axios 客户端、核心读服务和高频首屏页面补入 `AbortSignal` 与取消请求识别，修复快速切页旧请求 timeout 串页、取消请求误报 console、审计页未捕获 Promise 变成 pageerror 的问题；5001 标题与进程归属复核为爱劳达，CDP 18 路由回归从 `passed_with_warnings` 收口为干净 `passed`，无 HTTP 失败、无 console warning、无 fatal console、无 pageerror；核心 smoke、前端构建、runtime 健康、全仓审计均已通过，lint 当时为 0 error、13 warning
42. Lint 零警告与旧请求串页收口 - 已将剩余 13 个 React lint warning 收口为 0 warning，并在浏览器复核中捕获和修复两个真实运行细节：`#samples` 离页后的 `/api/samples` 旧请求 401 串到 `#production`，以及回款中心聚合加载 `promise.finally(...)` 未接住导致 timeout 冒成 `pageerror`；最终 `npm run lint`、`npm run build`、CDP 18 路由、核心 smoke、runtime 健康、全仓审计均通过，P1 仍为 0
43. 采购浏览器写入闭环与收货批次验收收口 - 已修复采购页初始加载晚返回覆盖新建供应商/采购单的问题，并将采购浏览器验收从旧“一键收货”升级为真实“收货批次抽屉 + stockEntryRef/batchNo 库存凭证核验”；采购浏览器真实写入、核心 CDP 烟测、核心 smoke、订单/采购/化工 BOM/货抵 API 闭环、runtime 健康、前端构建、lint、全仓审计均通过，P1 仍为 0
44. 稳定运行链、AI 安全与离线打包收口 - 已修复 AI 安全词源码乱码、AI 配置乱码、后端审计中间件被乱码注释吞掉、稳定打包断链、runtime 自检过浅、service worker 假离线、Docker 前端假完整、dual-port 稳定模式误报、HTTP 分块中文假乱码和 eslint 扫描打包产物卡住；稳定入口 5001、runtime 资源链、AI 安全、dual-port 编码、CDP 核心页面、订单、采购、货抵、化工 BOM、备份恢复、动态 RBAC、CRM/AI、collection 性能、并发对账、lint、前后端构建、有效源码禁乱码、全仓审计、稳定包生成、稳定包启动、稳定包资源自检和稳定包浏览器烟测均已通过；剩余为 P2/P3 拆分和 Docker build 实跑
45. 发布验收总闸门与审计范围收口 - 已新增有效源码禁乱码闸门与 `verify:release` 串行发布验收闸门，完整链路 18 步全部通过；修复全仓审计误扫 `AilaoDa_Stable_Package*` 运行产物导致 P1 假红的问题；已将 8 个无引用临时探针/一次性修复脚本、旧多租户迁移工具、旧 RBAC 测试脚本、根目录旧 `sw.js`、旧压缩脚本、旧 `mockAdapter` 移入隔离区；货抵真实页正名为 `BarterWorkspaceView`；新增 `utils/clientIssue.ts` 收敛回款与发货 Hook 的 console 噪声；启动器不再打印密码；新增 `utils/apiMapping.ts` 并收口订单、物流、客户、采购、调整单、本地 AI 规则服务的 `any` 映射边界；`test:smoke` 改走受保护 CDP 核心页面审计，生产后台审计不再默认跳过浏览器探针，dual-port 报告明确稳定模式只验证 5001；新增 `authRoute()` 并收口 collection/barter/production/warehouse/adjustment/asset/role/dashboard 路由类型边界；剩余为后端事务/错误/SQL row 类型边界、大文件拆分与 Docker 实跑证据
46. 后端事务类型边界首包 - 已导出统一 `TransactionClient` 并收口货抵库存辅助层 `barter.stock.ts` 的事务 client、结算单和明细类型，未改货抵主业务顺序；后端构建、前端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 42
47. 后端错误边界样板 - 已扩展 `AppError` 支持结构化 details，并将采购收货批次、采购状态更新、B2B 状态同步三处从动态 `error as any.statusCode/details` 迁移到 `AppError`；未改变采购收货、库存入账和差异单业务顺序；后端构建、前端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 42
48. 采购/发货/差异错误边界统一包 - 已将发货签收批次和收发货差异容差阻断也迁移到 `AppError`，与采购收货批次形成同一错误边界；定点扫描确认采购、发货、差异三处不再存在动态 `error as any` 状态挂载；后端构建、前端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 42
49. 采购/发货 SQL Row Mapper 与 Prisma Where 类型化 - 已将采购收货批次、发货签收批次 SQL row 从 `Record<string, any>` 收束为明确类型，并将采购供应商/采购单/发货列表 where 与供应商 create data 接入 Prisma 类型；类型化过程中修复发货列表 `status` query 未先字符串归一的真实隐患；后端构建、前端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 40
50. 货抵前端服务映射边界 - 已将 `services/barter.service.ts` 从 `any` API 读写映射改为 `unknown -> mapper -> 强类型对象`，并将过账返回值收束为 `ApiRecord`；未改货抵公式、过账、冲销和页面交互；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 39
51. 本地 AI / 表格识别类型边界 - 已确认 `TableImport.tsx` 乱码为 PowerShell 显示层误解码，未误伤源码；`services/freeAIService.ts` 已从表格解析、规则回复、风险洞察、合同/标签分析返回值层面清除代码级 `any`，AI 规则回复只读取汇总字段不读取业务明细；前端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过，AI 安全回归通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 38
52. CRM 导入与详情编辑类型边界 - 已将 `pages/crm/useCRM.tsx` 的 Excel/表格导入行从 `any[]` 收束为 `ImportedCustomerRow[]`，联系人导入改为 `unknown -> normalizeContactRecord -> Contact`，地址导入走统一归一化入口；CRM 详情抽屉的联系人/地址字段更新改为按字段泛型约束，`CRMCustomerDrawer.tsx` 不再暴露 `any` prop；前端构建、后端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 37
53. 客户后端访问与 payload 类型边界 - 已将 `customer.access.ts` 的客户范围 where 从 `Record<string, any>` 收束到 `Prisma.CustomerWhereInput`，数据范围参数改为 `DataScope`；`customer.payload.ts` 统一使用 `unknown -> helper -> 字段值` 处理联系人、别名、三语名称、地址、订单统计和客户 payload；`customer.persistence.ts` 的事务和地址/联系人持久化入参改为 Prisma transaction 与明确输入；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 34
54. RMA / 调账旧请求串页与稳定 runtime 收口 - 已确认上一轮 CDP `#adjustment` 失败不是业务编译失败，而是 5001 仍服务旧前端 chunk，导致旧 RMA 请求 timeout 串到后续页面；本包为 `RMA.tsx`、`rma.service.ts`、`AdjustmentCenterView.tsx`、`adjustment.service.ts` 补齐 `AbortSignal`、取消错误识别和 `unknown -> mapper -> RmaRecord` 边界，并用 `start-stable-v2.ps1` 安全重启爱牢达 5001 运行链让新 dist 生效；复核 5001 归属为爱牢达、5173/518x 为智仔未触碰；CDP 18 路由从 `passed_with_warnings` 收口为干净 `passed`，release core 9/9 通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-05-13-767Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 32
55. 后端调账服务事务与 where 类型边界 - 已将 `backend/src/services/adjustment.service.ts` 的事务 `tx:any`、调账记录 `adjustment:any`、列表/汇总 `Record<string, any>` 收束为 `TransactionClient`、`AdjustmentRecord` 和 `Prisma.AdjustmentRecordWhereInput`；未改变财务调账、生产/库存调账、应用、冲销的状态机与执行顺序；中途发现长 `Pick<>` 类型块会新增大文件 P2，已反向收敛为 `type PersistedAdjustment = AdjustmentRecord`，避免为了类型安全制造行数债；后端构建、前端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 31
56. 订单工作台详情/导入/导出类型边界 - 已将 `backend/src/services/order-workspace.service.ts` 的订单详情 formatter 从 `any` 改为 Prisma include 推导类型，列表与导出筛选从动态对象改为 `Prisma.OrderWhereInput`，批量导入从 `orders:any[]` 改为 `unknown -> ImportOrderRow`，导出合同号不再通过 `(o as any)` 读取；类型化过程中暴露并修复 `createdAt` 日期范围不能随意 spread 的真实隐患，改为显式 `Prisma.DateTimeFilter`；文件保持 499 行，没有新增大文件 P2；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 30
57. 生产完工/BOM 消耗校验类型边界 - 已将 `backend/src/services/production-mutation.service.ts` 的成品库位事务 client、BOM 校验项、库存快照、替代料组、消耗记录和工单更新 data 从 `any`/`Record<string, any>` 收束为 `TransactionClient`、`BomValidationItem`、`StockValidationSnapshot` 与 `Prisma.ProductionWorkOrderUpdateInput`；未改变 10 原料 BOM 覆盖校验、用量上下限、单位匹配、完工入库、原料扣减和成本台账顺序；类型化过程中暴露 BOM `unit` 可能为 null 的结构化问题，已用 `undefined` 表达空单位；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 29
58. 收发货差异 raw SQL row 与事务类型边界 - 已将 `backend/src/services/receipt-discrepancy.service.ts` 的 raw SQL 行从 `Record<string, any>` 收束为 `RawRow = Record<string, unknown>`，统一通过 `toRowString`、`toNullableNumber`、`toRowBoolean`、`normalizeEnumValue` 映射 case/rule/action；所有 tx 入参改为 `TransactionClient`，`$queryRawUnsafe` 改用 `RawRow[]` 泛型；未改变采购拒收、客户短签、容差规则、差异动作、结案/解决的 SQL 与业务顺序；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 28
59. 货抵/资产批次类型边界收口 - 已将 `backend/src/services/barter.service.ts` 的协议进度同步、货抵摘要、协议列表、执行批次列表从 `any` 收束为 `TransactionClient` 与 Prisma where/推导结果；`barter.types.ts` 的查询 where 改为 Prisma 条件类型；`barter.formatters.ts` 的货抵回款 metadata 改为明确结构；`barter.controller.ts` 创建货抵单/协议/批次审计日志不再通过 `as any` 猜字段；`asset.controller.ts` 的产品批次 CRUD 从 `(prisma as any).productBatch` 回到正式 `prisma.productBatch` 与 `Prisma.ProductBatchUpdateInput`；未改变换货贸易金额、分批抵扣、库存凭证、回款联动和批次有效期规则；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 25，且均为大文件拆分维护债
60. 本地 AI 表格识别拆分与旧请求 401 防踢登录 - 已将 `services/freeAIService.ts` 的表格类型识别、客户/订单/样品/物流解析、字段映射和上下文数字辅助拆到 `services/freeAI/tableRecognition.ts`，原服务从 669 行降至 282 行，新模块 358 行，保留原 `recognizeAndParseTable` 与类型导出；release core 首次复核暴露 `shipping` 离页旧资产请求 401 会清空当前登录态，已将 `utils/api.ts` 的 401 判断收紧为“请求携带当前 token 被拒”或“当前本来没有会话”才清登录，避免无 requestToken 的旧请求误踢；前端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 24
61. 后端校验层客户/生产域拆分 - 已将 `backend/src/validators/index.ts` 中的客户主数据校验拆到 `backend/src/validators/customer.ts`，生产/BOM/调账校验拆到 `backend/src/validators/production.ts`，保留 `validators/index.ts` 作为兼容出口，所有旧 import 无需改动；`index.ts` 从 928 行降至 590 行，客户校验 90 行，生产校验 255 行；后端构建、前端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 23
62. 回款中心加载闭环拆分 - 已将 `pages/collections/useCollectionCenter.ts` 内的聚合读模型、7 接口回退、inflight 去重和回退错误汇总拆到 `pages/collections/collectionLoadBundle.ts`，主 hook 从 636 行降至 540 行；未改变回款权限、催收、核销、承诺付款、争议、拦截释放和排序筛选逻辑；前端构建、lint、有效源码禁乱码、全仓审计和 release core 均通过；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 22
63. 收发货差异工作台配置拆分与风控聚合读模型 - 已将 `pages/ReceiptDiscrepancyWorkbench.tsx` 的标签、选项、搜索文本和数字格式拆到 `pages/receiptDiscrepancyWorkbench.config.ts`，主页面从 606 行收口到 589 行；release core 首次复核暴露 `#risk` 快速切页下偶发 `pageerror: timeout of 10000ms exceeded`，已将 `pages/RiskControl.tsx` 从 3 条回款接口并发读改为 `collectionsService.getWorkbench()` 聚合读模型，减少旧请求串页与超时；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T05-00-00-497Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 21
64. 销售订单 hook 拆分与资产旧请求取消链收口 - 已将 `pages/sales-orders/useSalesOrders.ts` 的表单初值、金额计算、Excel 明细解析、智能填充、OCR 应用、扫码合并和导入 payload 构造拆到 `pages/sales-orders/salesOrderFormHelpers.ts`，主 hook 从 930 行降至 599 行；release core 首次复核暴露 `#assets` 离页后的 `/api/assets/history` 401 串到 `#adjustment`，已为 `pages/useAssets.tsx` 的余额、历史和批次读取补齐 `AbortController`、取消错误识别和卸载后不回写；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T05-19-10-761Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 20
65. 回款服务类型边界拆分 - 已将 `backend/src/services/collection.service.ts` 中的回款快照、承诺付款、争议、冻结记录类型和客户展示名 helper 拆到 `backend/src/services/collection/collection.types.ts`，主服务从 643 行降至 525 行；未改变回款汇总、逾期分桶、催收提醒、承诺付款、争议、客户/订单冻结释放的业务顺序；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T05-25-03-351Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 19
66. 生产完工 BOM 消耗校验拆分 - 已将 `backend/src/services/production-mutation.service.ts` 中的 BOM 消耗覆盖、单位匹配、用量上下限、替代料组和结构化完工校验错误拆到 `backend/src/services/production-completion.validation.ts`，主服务从 729 行降至 493 行；未改变 10 原料 BOM 覆盖校验、完工入库、原料扣减、成本台账和工单状态机顺序；前后端构建、lint、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T05-30-23-239Z.json`；化工 BOM 生产链专项通过，少耗 9 种原料被阻止，补齐后生成原料消耗/成品入库/成本台账，报告为 `F:\爱牢达\output\playwright\chemical-bom-production-chain-audit-report-v1.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 18
67. 核心业务浏览器验收与并发对账收口 - 已启动并复核稳定入口 `http://127.0.0.1:5001/`，确认运行时 DB 为 `D:\AilaoDaRuntime\stable.db`；CRM 主数据、采购、销售订单、回款、货抵协议分批执行、生产、发货、调账、备份恢复、并发对账均完成本轮真实脚本验收。生产浏览器验收首轮失败根因为审计脚本在质检保存后立即读取 API，未等待 `qualityChecks` 回写，已改为按质检备注轮询读回，属于验收脚本假失败修复，不改变生产业务代码；并发对账首轮 `Customer not found` 未复现，最小诊断证明客户创建、读回、订单创建链路正常，完整并发脚本复跑 3 个冲突场景 0 漂移。`npm run lint`、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-06-23-642Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 18
68. 并发对账审计脚本拆分 - 已将 `scripts/concurrency-reconcile-deep-audit-v1.cjs` 的测试数据、API client、库存/货抵/发货辅助函数拆到 `scripts/lib/concurrency-reconcile-audit-utils.cjs`，主脚本从 768 行降至 570 行，低于 600 行 P2 阈值；拆分后订单重复回款、货抵批次/过账重复提交、发货出库/签收回执重复提交 3 个冲突场景复跑通过，0 漂移。`npm run lint`、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-14-08-635Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 17
69. 旧 timber 接口断开与标记 - 已将 `/api/timber/*` 从旧木材业务真实接口改为 410 Gone 禁用壳，统一返回 `LEGACY_TIMBER_API_DISABLED`、`replacement=/api/barter`、`disabledAt=2026-04-20`；保留 `backend/src/controllers/timber.controller.ts` 作为历史参考并标记 `LEGACY DISCONNECTED`，不再作为运行链入口；`role-read-permission-audit-v1.cjs` 已改为验证 timber summary 对全部角色返回 410；新增 `legacy-interface-disconnect-audit-v1.cjs` 验证 calculate、summary、convert-po 匿名和角色访问均返回 410。后端构建、runtime、自专项旧接口断开审计、角色读权限、lint、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-23-10-936Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 17
70. 旧 timber 前端入口与旧 mock 审计口径收口 - 已确认正式壳层会把 `#timber` 归一到 `#barter`，`activePathRegistry` 不再挂载旧 `TimberWorkspace`；本包将 `pages/TimberWorkspace.tsx` 降级为安全兼容 import shell，任何误导入都会进入正式 `BarterWorkspaceView`，不再发起 `/api/timber/*` 请求；`TimberController` 已从历史计算/查询/转采购实现降级为 410 禁用控制器，防止未来误接 route 后旧逻辑复活；`TimberEngine` 与旧 timber schema 已标记 `LEGACY DISCONNECTED`；英/越/中三语 `timber` 口径已从木材/板材改为货抵支付/换货贸易；全仓审计脚本补充识别 `backend/simple-server.js` 为已禁用旧 mock，`disabledLegacyFiles` 从 1 变为 2。前端构建、后端构建、lint、旧接口断开专项、有效源码禁乱码、全仓审计、runtime 和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-37-49-647Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 17
71. 旧入口判定表与误运行入口禁用 - 已新增 `旧入口判定表_2026-04-20.md`，将旧 API、旧页面、旧 controller、旧 mock、旧一次性 refactor、旧数据库修复脚本、旧验收委托入口、旧数据兼容字段和重复命名分层判定；本包只处理会误写源码或误写数据库的入口，`refactor.cjs`、`refactor2.cjs`、`backend/scripts/fix-db.js`、`backend/scripts/fix-db.ts` 均加禁用壳并指向当前正式入口；`full-codebase-audit-v1.cjs` 已识别这些为 `disabledLegacyFiles`。旧入口快速退出验证、后端构建、lint、有效源码禁乱码、全仓审计、runtime 和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-46-03-927Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 17，`legacyNamedFiles=1` 且为当前旧接口断开专项审计脚本
72. 化工 BOM 生产链审计脚本拆分 - 已将 `scripts/chemical-bom-production-chain-audit-v1.cjs` 的报告、HTTP 请求、仓库/库存/生产 API 辅助函数拆到 `scripts/lib/chemical-bom-audit-utils.cjs`，主脚本从 634 行降至 390 行，保留 10 原料 BOM、少耗阻断、补齐完工、原料扣减、成品入库、成本台账回读的场景编排；拆分后禁乱码闸门捕获主脚本测试数据中残留源码层乱码，已按三步法改为 ASCII 内部代号测试数据，不改业务逻辑。化工 BOM 专项通过，报告为 `F:\爱牢达\output\playwright\chemical-bom-production-chain-audit-report-v1.json`；lint、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-55-54-019Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 16
73. 后端业务链审计脚本拆分 - 已将 `scripts/backend-business-chain-audit-v1.cjs` 的 HTTP 请求、登录、供应商/客户/订单/采购/发货/库存读回辅助函数拆到 `scripts/lib/backend-business-chain-audit-utils.cjs`，主脚本从 835 行降至 420 行，helper 为 501 行；拆分过程中发现测试脚本源码层坏中文残留，已按三步法确认是审计测试数据层问题，将仓库匹配改为稳定 `WH-MAIN` 代码匹配，并将供应商别名改为 ASCII 内部代号，不改业务接口和业务数据结构。后端业务链专项通过，覆盖登录、仓库库位、供应商、客户、销售订单、订单确认、回款提交/核验、采购单、采购收货、差异单、原料库存、成品库存、发货出库、签收回执、订单发货后读回；lint、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T14-04-05-005Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 15
74. 浏览器人工流审计脚本拆分与登录兼容 - 已按三步法确认 `scripts/browser-human-flow-audit-v1.cjs` 在 PowerShell 中显示的乱码属于 UTF-8 无 BOM 被控制台误解码，Node 直接读取为正常中文；将测试数据工厂、登录、截图、超时、路由断言、模块记录等工具层拆到 `scripts/lib/browser-human-flow-audit-utils.cjs`，主脚本从 811 行降至 543 行，helper 为 338 行；修复旧脚本强制等待登录角色下拉的问题，当前登录页无角色下拉时按用户名登录。浏览器实跑已不再卡死，但暴露旧人工流脚本与当前 UI 脱钩：CRM 详情抽屉读回、订单新建弹窗选择器、发货签收前置数据、仓储确认入库按钮匹配、调账 Tailwind class 选择器均需后续按当前页面锚点重写；本包不把该脚本计为业务验收通过。lint、有效源码禁乱码、全仓审计和 release core 9/9 均通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T14-18-24-244Z.json`；当前全仓审计 P0/P1/P3 为 0，仅剩 P2 14

## 当前执行原则
- 先治理，再补主线
- 先收口，再扩展
- 主线只做闭环，不再重复造子系统
- 支撑模块保留，但不抢主线
- 每包先定边界，再改代码
- 每包结束必须有证据和反思
