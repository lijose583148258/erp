# 生产工作台前端表单状态拆分记录 2026-04-21

## 范围
- 子任务：A，生产工作台前端结构治理。
- 写入范围：`pages/ProductionWorkspaceV2.tsx`、`pages/production/*`、本治理记录。
- 目标：降低 `ProductionWorkspaceV2.tsx` 维护风险，不改变生产业务语义。

## 三步法判断
1. 翻译/识别：PowerShell 输出中曾出现中文显示异常，但 Node UTF-8 读取 `newStep('备料') / newStep('生产') / newStep('质检')` 正常。
2. 判断层级：属于终端显示层风险，不是本轮需要修复的源码乱码。
3. 修复策略：本轮不改中文业务文案，只抽离结构。

## 变更
- 新增 `pages/production/useProductionWorkspaceForms.ts`。
- 将 BOM 表单状态、工单表单状态、质检表单状态、生产调账表单状态从主页面抽离到专用 hook。
- `pages/ProductionWorkspaceV2.tsx` 保留数据加载、选中对象、核心业务动作函数和页面拼装。

## 未改动的核心链路
- 未改 `handleCompleteWorkOrder`。
- 未改库存扣料参数。
- 未改 BOM 10 原料规则。
- 未改工单状态流转语义。
- 未改质检创建接口语义。
- 未改生产调账创建/冲销接口语义。

## 行数变化
- `pages/ProductionWorkspaceV2.tsx`：585 行降至 527 行。
- 新增 `pages/production/useProductionWorkspaceForms.ts`：174 行。

## 验证
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过，无 warning。
- `node scripts/effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts/full-codebase-audit-v1.cjs`：通过，P0/P1/P2 findings 为 0，超大文件为 0，可疑旧命名文件为 0，可疑重复名组为 0。

## 运行层说明
- 本轮触碰了生产工作台运行页面源码。
- 因为当前是多智能体并行任务，本子任务没有重启主控稳定服务，避免覆盖其它子任务的运行态。
- 主控汇总后应统一执行 `npm run build`、稳定服务重启、`scripts/check-runtime.ps1`、生产浏览器审计，确认浏览器读取的是最新 bundle。

## 结论
本轮属于低风险结构拆分。生产核心业务链路未被改写，维护风险下降；运行层最终结论需要主控在所有并行子任务合并后统一验收。
