# Git 基线与采购领域服务拆分记录（2026-04-21）

## 背景

- 用户提供历史仓库：`https://github.com/lijojo406-source/ERP-A-I-`
- 本机当前目录 `F:\爱牢达` 原本不是 Git 工作区，没有 `.git`，无法用 `git diff` 稳定保护后续修复。
- 通过 `git ls-remote` 读取远程时返回 `Repository not found`，当前判断为：仓库可能是私有、URL 不完整，或本机没有 GitHub 授权。

## 已执行的非破坏式动作

- 初始化本地 Git 仓库，仅用于当前目录的本地版本边界。
- 未配置远程地址，未推送任何内容到 GitHub。
- 补充 `.gitignore`，忽略：
  - `node_modules/`
  - `dist/`
  - `output/`
  - `logs/`
  - `uploads/`
  - `backups/`
  - 历史包 `AilaoDa_Stable_Package*`
  - 本地 SQLite / runtime 数据库：`*.db`、`*.sqlite`、`backend/prisma/tenants/` 等。
- 已从 Git 索引移除本地 SQLite 数据库文件，磁盘文件仍保留，没有删除用户数据。
- 添加本地 `.git/hooks/pre-push` 保险钩子：在历史清理前禁止推送。

## 需要诚实说明的风险

- 初始化第一笔本地提交时，曾短暂把部分 SQLite 运行库副本纳入本地 Git 历史。
- 已在第二笔提交从当前索引移除，但本地历史里仍可追溯到第一笔提交。
- 因此：在连接远程或推送前，必须先执行一次 Git 历史脱敏/重建，确保数据库文件不出现在任何提交历史里。
- 当前没有远程地址，且 pre-push 钩子已阻止推送，所以没有远程泄露。

## 采购领域服务拆分

本包完成第一刀低风险拆分：

- 新增领域服务：`backend/src/services/procurement-domain.service.ts`
- 从 `backend/src/controllers/procurement.controller.ts` 移出以下纯规则：
  - 供应商别名解析与序列化
  - 多联系人/多地址 JSON 列表归一化
  - 供应商展示名
  - 供应商搜索条件
  - 供应商 DTO
  - 采购单 DTO
  - 币种归一化
  - 到岸成本计算
  - 收货单位成本与入库成本金额计算
  - 采购状态标准化
  - 采购状态转换规则

## 效果

- `procurement.controller.ts` 从约 1208 行降到 978 行。
- 控制器减少 242 行规则和 DTO 噪音。
- 控制器仍保留权限、HTTP、事务编排，不一次性拆散路由，避免大面积风险。

## 验收证据

- `npm --prefix backend run build`：通过。
- `node .\scripts\effective-source-mojibake-gate-v1.cjs`：通过，扫描 376 个当前有效源码文件，发现 0。
- `npm run audit:active-source`：通过，当前有效源码 377 个。
- `npm run audit:human-flow -- --module=procurement-warehouse`：通过。
  - 报告：`F:\爱牢达\output\playwright\browser-human-flow-audit-report-v1.json`
  - 证据截图包含：
    - `procurement-supplier-created.png`
    - `procurement-order-created.png`
    - `warehouse-inbound-readback.png`
- `npm run verify:release:core`：10/10 通过。
  - 报告：`F:\爱牢达\output\audit\release-verification-v1-core-2026-04-21T04-41-59-008Z.json`

## 下一步

1. 在不连接远程前，继续本地小包拆分。
2. 若要真正接入 GitHub，先做历史脱敏/重建，不允许直接推送当前本地历史。
3. 下一刀建议拆 `procurement.controller.ts` 的收货事务服务，把 `createReceipt` 的事务体迁移到服务层。
4. 再之后拆 `pages/Procurement.tsx`，分成供应商列表、供应商表单、采购单列表、收货动作区。
