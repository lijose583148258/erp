# P3 最终发布记录

日期：2026-06-01

## 发布包

- 最终包：`F:\爱牢达\output\release\AilaoDa_Clean_Runtime_P3_FINAL_20260601_165859.zip`
- 纯净运行目录：`E:\爱劳达纯净系统`
- 入口：`http://127.0.0.1:5001/`
- 运行数据库：`D:\AilaoDaRuntime\stable.db`

## 本轮完成范围

### P0

- 取消移动端禁止缩放。
- 增加默认紧凑模式。
- 触屏设备表格操作按钮常显。
- 提升表格文字和辅助文字对比度。
- 移除固定 JWT fallback。
- 生产环境强制要求 JWT_SECRET。
- 生产 seed 默认拒绝演示弱口令账号。
- 新增 `bootstrap-admin` 初始化管理员命令。
- 子账号链路通过：管理员登录、创建员工、员工登录、禁用员工、禁用后拒绝登录。

### P1

- 新增 Workflow 运行时表。
- 新增审批定义、审批实例、待办、审批动作 API。
- 新增站内通知表和 API。
- 新增 BI 摘要 API。
- 新增预警执行 API。
- 风控中心新增 Commercial Ops 面板，可查看审批、通知、BI、预警。

### P2

- 新增 Prometheus `/metrics` 指标接口。
- 增加部署模式：`local`、`private`、`saas`。
- SaaS 模式禁止 SQLite 启动。
- PostgreSQL URL 在当前 SQLite Prisma 构建下明确拒绝启动，避免假装支持云 SaaS。
- Docker compose 增加部署模式环境变量。

### P3

- 核心发布验收通过。
- 重新生成稳定包。
- 重新部署 E 盘纯净运行目录。
- 生成最终可移动 zip。
- 从 E 盘纯净目录启动后完成健康接口、metrics、浏览器风控页复验。

## 验收记录

- `npm run verify:release:core`：通过，19/19。
- 发布验收报告：`F:\爱牢达\output\audit\release-verification-v1-core-2026-06-01T09-55-16-366Z.md`
- `/api/health`：通过。
- `/metrics`：通过。
- Playwright 风控页验证：通过，确认 Commercial Ops 面板可见且无控制台错误。
- zip 内容检查：通过，未包含 previous、logs、output、runtime-data 污染目录。

## 重要限制

当前最终包仍是本地 / 私有化 SQLite 运行包，不是云 SaaS PostgreSQL 包。

如需云 SaaS，下一阶段必须生成 PostgreSQL Prisma 构建、迁移脚本、读写分离配置和租户隔离测试。

