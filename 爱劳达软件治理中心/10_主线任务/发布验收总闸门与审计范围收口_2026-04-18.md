# 发布验收总闸门与审计范围收口（2026-04-18）

## 目标

把原来分散的构建、运行、浏览器、API、权限、AI、安全、并发、备份恢复检查收成一套可复跑的发布验收闸门，避免“单个脚本通过就误判稳定”的假绿问题。

## 本包改动

- 新增 `scripts/effective-source-mojibake-gate-v1.cjs`：只扫描当前有效源码，不扫描稳定包、历史归档、治理文档、日志、备份、上传、隔离脚本，避免报告层/历史层误报成源码乱码。
- 新增 `scripts/run-release-verification-v1.cjs`：串行执行核心与完整发布验收，每一步独立 300 秒上限，超时即标记 `stuck`。
- 新增 `npm run verify:release:core`：核心验收，适合每次小改后快速复核。
- 新增 `npm run verify:release`：完整验收，适合每个稳定包、上线前、EXE 打包前复核。
- 修复 `scripts/full-codebase-audit-v1.cjs` 的扫描范围：`AilaoDa_Stable_Package*` 属于运行产物/历史快照，不再计入活跃源码。

## 完整验收结果

- 命令：`npm run verify:release`
- 总步骤：18
- 通过：18
- 失败：0
- 卡住：0
- 最长步骤：`concurrency-reconcile-serial`，约 30 秒
- 每步上限：300 秒

## 证据路径

- 完整发布验收 JSON：`F:\爱牢达\output\audit\release-verification-v1-full-2026-04-18T17-09-30-732Z.json`
- 完整发布验收 Markdown：`F:\爱牢达\output\audit\release-verification-v1-full-2026-04-18T17-09-30-732Z.md`
- 全仓审计 JSON：`F:\爱牢达\output\audit\full-codebase-audit-v1.json`
- 全仓审计 Markdown：`F:\爱牢达\output\audit\full-codebase-audit-v1.md`

## 当前判定

- 当前稳定入口仍为 `http://127.0.0.1:5001/`。
- `5173/5180/518x` 归属智仔或其他 Vite 进程，不属于爱牢达稳定入口。
- 当前有效源码禁乱码闸门通过。
- 当前全仓审计 P0/P1 为 0。
- 当前完整发布验收通过，但这不等于“永远不会有业务细节问题”，只能说明本轮覆盖到的稳定运行、核心页面、构建、lint、AI 安全、订单、采购、货抵、化工 BOM、备份恢复、动态角色、CRM/AI、回款性能、并发对账链路均未发现阻塞。

## 后续剩余项

- P2：大文件拆分仍需要按模块单独做，不应该混在稳定收口包里强拆。
- P2：Docker build/run 实证仍缺，因为当前机器未检测到 Docker CLI。
- P3：临时探针、一次性修复脚本、历史快照目录后续可归档，但必须先确认不再被引用，不能直接删除。

## 追加收口：临时脚本隔离

已先做引用检查，确认以下脚本没有被正式入口或验收链引用，然后移动到 `scripts/quarantine`，未删除：

- `scripts/fix-ui.js`
- `scripts/fix-production-v2.cjs`
- `scripts/fix-production-v2.ps1`
- `scripts/tmp-chrome-launch-probe.cjs`
- `scripts/tmp-delayed-launch-probe.cjs`
- `scripts/tmp-edge-launch-probe.cjs`
- `scripts/tmp-plain-launch-probe.cjs`
- `scripts/tmp-sales-launch-probe.cjs`

隔离后复核：

- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1 为 0，P3 从 90 降到 82。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，当前有效源码未发现乱码。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck。

## 追加收口：前端 console 噪声治理

已新增 `utils/clientIssue.ts`，把页面 Hook 里的散落 `console.error/warn` 收敛为统一的客户端问题面包屑：

- 开发环境仍输出到 console，方便调试。
- 运行环境保留最近 50 条 `window.__AILAODA_CLIENT_ISSUES__`，避免用户界面出现调试噪声。
- 首包迁移 `pages/collections/useCollectionCenter.ts` 与 `pages/shipping/useShipping.ts`。

复核结果：

- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1 为 0，P3 从 82 降到 80。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck。

## 追加收口：P3 清零与旧工具隔离

本轮按三步法复核后确认：

- `backend/prisma/migrations/multiTenantMigration.ts` 是旧多租户迁移工具，文本层真乱码，且不在当前后端 `tsconfig` 编译范围，也未被当前包脚本引用。
- `backend/scripts/test-rbac.ts` 是旧 RBAC 测试脚本，文本层真乱码，且未被当前验收链引用。
- `scripts/start-stable-v2.ps1` 会打印 `admin / admin123`，属于运行脚本层的专业度与安全噪声。

处理动作：

- 已将旧多租户迁移工具隔离到 `backend/prisma/migrations/quarantine/multiTenantMigration.legacy-mojibake.ts`。
- 已将旧 RBAC 测试脚本隔离到 `backend/scripts/quarantine/test-rbac.legacy.ts`。
- 已将 `scripts/start-stable-v2.ps1` 的登录提示改为不打印密码。
- 已增强 `scripts/effective-source-mojibake-gate-v1.cjs` 对旧 GBK/UTF-8 错配符号的识别。
- 已调整 `scripts/full-codebase-audit-v1.cjs`：测试脚本演示账号进入 `testCredentialAssets` 统计，CLI/修复工具 console 输出进入 `consoleHeavyUtilityAssets` 统计，不再污染 P3 缺陷列表。

复核结果：

- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 74。
- `npm run lint`：通过。
- `npm --prefix backend run build`：通过。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck。

## 追加收口：正名、旧壳隔离与 P2 噪声压缩

已继续处理不会改变业务口径的维护噪声：

- 根目录旧 `sw.js` 是 pass-through 旧壳，正式 Service Worker 已由 `public/sw.js` 提供；已隔离到 `scripts/quarantine/sw.legacy-pass-through.js`。
- `pages/BarterWorkspace.clean.tsx` 是货抵当前真实实现，文件名继续叫 `clean` 会误导维护者；已正名为 `pages/BarterWorkspaceView.tsx`，并保留 `pages/BarterWorkspace.tsx` 兼容入口。
- `app/activePathRegistry.ts` 已同步改为动态导入 `BarterWorkspaceView`。
- `build_clean_zip.ps1` 是旧压缩脚本，路径乱码且含删除动作，当前无人引用；已隔离到 `scripts/quarantine/build_clean_zip.legacy-mojibake.ps1`。
- 全仓审计规则已区分正常的前后端同名分层、常见 barrel 文件、受控运行脚本、CLI console 输出与测试演示账号，不再把它们混进缺陷列表。

复核结果：

- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 56。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck。

## 追加收口：旧 Mock 后端隔离

`utils/mockAdapter.ts` 是旧前端 mock 后端，存在大量历史乱码 mock 文案，也会在 `VITE_USE_MOCK=true` 时绕开真实后端。按“本地稳定运行必须走真实后端”的原则，本轮将其降级为隔离资产：

- 已移动到 `utils/quarantine/mockAdapter.legacy-mojibake.ts`。
- `utils/api.ts` 已移除静态 mock adapter 引用。
- 即使设置 `VITE_USE_MOCK=true`，开发环境也只提示该开关被忽略，不再切到假数据。

复核结果：

- `npm run build`：通过，主入口包从约 379KB 降到约 348KB。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 54。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck。

## 追加收口：前端服务类型边界与验收入口治理

本轮继续压缩剩余 P2，但没有直接拆大控制器或重写业务链。处理原则是：先收束前端 API 回包映射层，避免页面“看似能显示、实际字段丢失或 NaN”的隐性问题。

已完成：

- 新增 `utils/apiMapping.ts`，统一提供 `unknown` 到对象、数组、字符串、数字的安全读取 helper。
- `services/order.service.ts` 已移除 `any` 映射，订单、回款记录、历史日志回读统一走安全转换。
- `services/shipping.service.ts` 已移除 `any` 映射，物流、签收、回单、差异 bundle 数量字段统一防 `NaN`。
- `services/customer.service.ts` 已抽出 `mapCustomerResponse`，合并客户列表、创建、更新、客户池更新的重复映射，同时保留三语名称、多地址、多联系人、公海/私海字段。
- `services/procurement.service.ts` 已移除采购前端服务的 `any` 映射，并新增 `LinkedSalesOrderRef`，避免 B2B 关联把销售订单边界继续写成无约束对象。
- `package.json` 的 `test:smoke` 已改为受保护的 `scripts/cdp-core-pages-smoke-audit-v1.cjs`，不再走根目录旧 Playwright smoke。
- `package.json` 的 `audit:production:background` 不再默认设置 `BROWSER_GUARD_SKIP_PROBE=1`，避免后台审计跳过浏览器启动探针。
- `scripts/dual-port-audit-v2.cjs` 已补充 `auditMode`、`stableRuntimePort`、`frontend3000Compared`、`frontend3000Required` 和稳定模式说明，避免把“只验证 5001”误读成“双端口都通过”。

复核结果：

- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 50。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T02-53-02-920Z.json`。
- `npm run test:smoke`：通过，已确认 smoke 入口走核心 CDP 页面审计。

剩余不在本包硬拆：

- 后端大文件和路由 `req as any` 仍是 P2，需要单独做“后端类型边界收口包”。
- `schema.prisma`、采购/发货/货抵/收货差异大文件不应为了数字清零直接重写，必须先补事务、错误、路由和读回测试边界。

## 追加收口：后端路由类型边界与本地 AI 规则服务边界

本轮继续按“先边界、后拆分”的原则推进，没有重写后端大业务模块，也没有改采购、发货、货抵、生产等事务顺序。

已完成：

- `backend/src/middleware/auth.ts` 新增 `authRoute()`，统一把 Express `Request` 收束为 `AuthRequest`，避免路由层继续散落 `req as any`。
- `backend/src/routes/collection.routes.ts`、`barter.routes.ts`、`production.routes.ts`、`warehouse.routes.ts`、`adjustment.routes.ts`、`asset.routes.ts`、`role.routes.ts` 已迁移到 `authRoute()`。
- `backend/src/routes/dashboard.routes.ts` 已用 Prisma where 类型替代临时 `any`，统计 reduce 也不再使用 `any`。
- `services/adjustment.service.ts` 已把调整单快照从 `Record<string, any>` 收束到 `Record<string, unknown>`，并统一数值/字符串转换。
- `services/geminiService.ts` 已把本地规则 AI 的上下文改为 `unknown`/受限 record，减少无约束数据边界；这不是给 AI 扩权，而是减少它能随意读取未声明字段的风险。

复核结果：

- `npm run build`：通过。
- `npm --prefix backend run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 43。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T03-02-34-429Z.json`。

下一包仍不建议直接大拆文件。优先级应为：

1. 后端事务 client 类型、BusinessError/HttpError 统一。
2. 采购/发货/收货差异 raw SQL row mapper 类型化。
3. 前端 `services/barter.service.ts`、`freeAIService.ts`、`pages/crm/useCRM.tsx` 的剩余类型边界。
4. 最后再拆大文件和 Prisma schema 文档化。

## 追加收口：货抵库存事务类型边界首包

本轮没有重写货抵主服务，也没有调整货抵过账、冲销、库存凭证的业务顺序；只收口库存凭证辅助层的事务和明细类型边界，避免后续继续把库存交易写成 `any`。

已完成：

- `backend/src/services/stock-movement.service.ts` 导出 `TransactionClient`，复用 Prisma transaction client 类型，不再让下游模块各自临时声明。
- `backend/src/services/barter/barter.stock.ts` 新增 `BarterStockSettlement`、`BarterStockItem`，并将 `resolveBarterLocationId()`、`buildBarterStockLine()`、`postBarterStockEntries()`、`postBarterStockReversalEntries()` 的 `tx` 和明细入参从 `any` 收束到明确类型。
- 这次只处理货抵库存辅助层，避免为了 P2 数字直接碰货抵主流程大文件。

复核结果：

- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 42。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T03-08-32-795Z.json`。

下一包继续按低风险价值排序：

1. 后端 `BusinessError/HttpError` 统一，先选一个控制器做样板。
2. 收货差异、采购、发货 raw SQL row mapper 类型化。
3. 前端 `services/barter.service.ts` 读写映射收口。
4. `freeAIService.ts` 和 `pages/crm/useCRM.tsx` 剩余 `any` 需要结合 AI 数据隔离与 CRM 页面回归，不宜机械替换。

## 追加收口：后端错误边界样板

本轮继续压缩后端“能跑但不稳”的隐性债务，重点不是为了 P2 数字立刻下降，而是建立可复制的错误处理样板。

已完成：

- `backend/src/middleware/errorHandler.ts` 的 `AppError` 增加 `details?: Record<string, unknown>`，支持业务错误携带安全结构化细节。
- `backend/src/controllers/procurement.controller.ts` 的采购收货批次、采购状态更新、B2B 状态同步三处，不再用普通 `Error` 动态挂载 `statusCode/details`。
- 超收、采购单不存在、已取消、未审批、已全部收货、部分收货必须走分批接口等错误，统一使用 `AppError` 表达 HTTP 状态和错误类型。
- 这次只改采购控制器内同类错误边界，未改变采购单、收货、库存入账、差异单创建的业务顺序。

复核结果：

- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 42。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T03-13-43-993Z.json`。

下一包继续：

1. `shipping.controller.ts` 同类 `statusCode/details` 动态错误迁移到 `AppError`。
2. `receipt-discrepancy.service.ts` 的业务阻断错误迁移到 `AppError`。
3. raw SQL 查询结果 row mapper 类型化，先处理采购/发货收货批次。

## 追加收口：采购/发货/差异错误边界统一包

本轮把“收货/签收/差异阻断”三条镜像链路的动态错误挂载统一到 `AppError`，减少上线后因为错误对象形状不一致导致前端拿不到状态码或结构化细节的风险。

已完成：

- `backend/src/controllers/shipping.controller.ts` 的签收批次链路，从动态 `(error as any).statusCode/details` 迁移到 `AppError`。
- 签收单不存在、已签收、未出库、状态不可签收、已全部签收、超签、签收文件非法，均统一用 `AppError` 表达状态。
- `backend/src/services/receipt-discrepancy.service.ts` 的容差阻断错误迁移到 `AppError`，保留 discrepancyType、quantity、toleranceRuleId、tolerancePercent、toleranceQuantity、varianceRate 等 details。
- 定点扫描确认 `procurement.controller.ts`、`shipping.controller.ts`、`receipt-discrepancy.service.ts` 不再存在 `error as any`、`.statusCode =`、`.details =` 动态挂载。

复核结果：

- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 42。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T03-16-48-411Z.json`。

下一包进入 raw SQL row mapper 类型化：

1. 采购收货批次查询 `purchase_receipts`。
2. 发货签收批次查询 `shipment_receipts`。
3. 收发货差异 case/action 查询结果。

## 追加收口：采购/发货 SQL Row Mapper 与 Prisma Where 类型化

本轮继续做数据边界，不改业务顺序。重点是把收货/签收 SQL 结果从 `Record<string, any>` 改为明确 row 类型，并让查询条件经过 Prisma 类型检查。

已完成：

- `backend/src/controllers/procurement.controller.ts` 引入 `TransactionClient` 和 Prisma 类型。
- 采购收货批次 `PurchaseReceiptRow`、`PurchaseReceiptTotalsRow` 已显式建模，`listPurchaseReceipts()`、`getPurchaseReceiptTotals()`、`buildPurchaseReceiptSummary()` 不再用 `Record<string, any>`。
- 采购收货批次新建后的 `receiptRows` 改为 `$queryRawUnsafe<Array<{ id: number }>>()`，不再用后置断言。
- 采购供应商搜索 where、采购单 where、供应商 create data 改为 `Prisma.SupplierWhereInput`、`Prisma.PurchaseOrderWhereInput`、`Prisma.SupplierCreateInput`。
- `backend/src/controllers/shipping.controller.ts` 引入 `TransactionClient` 和 Prisma 类型。
- 发货签收批次 `ShipmentReceiptRow`、`ShipmentReceiptTotalsRow` 已显式建模，签收列表与汇总不再用 `Record<string, any>`。
- 发货出库辅助函数 `syncOrderShipmentState()`、`hasShippingIssuePosted()`、`resolveShippingIssueStock()`、`postShippingIssueIfMissing()` 改用 `TransactionClient`。
- 发货列表 where 改为 `Prisma.ShipmentWhereInput`，并修复类型化后暴露的真实隐患：`req.query.status` 必须先 `String(status)`，不能直接塞给 Prisma where。

复核结果：

- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 40。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T03-23-12-302Z.json`。

下一包建议：

1. 前端 `services/barter.service.ts` 读写映射收口。
2. `freeAIService.ts` AI 输出边界与隐私字段白名单复核。
3. `pages/crm/useCRM.tsx` 拆成数据映射、筛选、动作三个小 hook，不能一口气重写。

## 追加收口：货抵前端服务映射边界

本轮只收口前端货抵 service 的 API 映射，不改货抵页面、不改货抵公式、不改过账与冲销业务流程。

已完成：

- `services/barter.service.ts` 引入 `ApiDataResponse`、`ApiRecord` 与安全转换 helper。
- 货抵条目、协议、结算单、列表、预览、汇总全部改为 `unknown -> mapper -> 强类型对象`。
- `BarterSettlement` 的 valuationSnapshots、offsetPostings、reversalLogs、paymentRecord 从 `Record<string, any>` 改为 `ApiRecord`。
- `post()` 返回值从 `Promise<any>` 改为 `Promise<ApiRecord>`；当前页面仅等待过账完成，不依赖任意结构。
- 定点扫描确认 `services/barter.service.ts` 不再存在 `any`。

复核结果：

- `npm run build`：通过。
- `npm --prefix backend run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 39。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T03-27-43-431Z.json`。

下一包继续看 P2 分布后决定，原则：

1. AI 服务先检查隐私白名单与输出 shape，再决定是否改。
2. CRM hook 只做拆分准备和 mapper 下沉，不直接大重写。
3. 后端剩余大控制器继续按“类型边界优先、拆文件最后”的节奏处理。

## 追加收口：本地 AI / 表格识别类型边界

本轮处理的是本地规则引擎 `services/freeAIService.ts`，不是外部 AI 调用。目标是减少本地表格识别、产品标签识别和规则回复中的 `any`，并保持“只返回汇总，不暴露明细”的 AI 安全边界。

三步法复核：

- 发现 `components/TableImport.tsx` 在 PowerShell 中显示乱码。
- 使用 Node UTF-8 读取后确认源码中文正常，属于 PowerShell 显示层误解码。
- 未对 `TableImport.tsx` 做误伤式重写。

已完成：

- 新增 `ParsedFormType`、`ParsedFormRecord`、`AnalyzedContractFile`、`AnalyzedProductLabel`。
- `ParsedFormData.data` 从 `any` 收束为 `ParsedFormRecord[]`。
- 客户、订单、样品、物流、通用表格解析从 `any[]` / `any[][]` 改为 `string[][]` 与 `ParsedFormRecord[]`。
- `processAICmd()` 的订单统计只读取 `totalAmount` 汇总，不读取订单明细。
- `getRiskInsight()` 入参从 `any` 改为 `unknown`，只按白名单读取 name、creditLimit、overdueAmount、overdueDays、totalOrders、totalAmount。
- `analyzeContractFile()`、`analyzeProductLabel()` 返回值从 `Promise<any>` 改为明确结构。
- 严格扫描确认 `services/freeAIService.ts` 不再存在代码级 `any`。

复核结果：

- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 38。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，包含 AI 安全回归，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T03-34-35-620Z.json`。

下一包建议：

1. `pages/crm/useCRM.tsx` 先做 mapper 和过滤条件类型边界。
2. `backend/src/services/receipt-discrepancy.service.ts` 继续做 SQL row mapper 类型化。
3. 后端客户 controller 拆分要等类型边界再降一轮后做。

## 反思

这次最关键的修正不是新增一个脚本，而是把“验收范围”从人脑记忆变成可执行规则。之前容易出现两个偏差：一是历史包被当成活跃源码导致假红，二是单个 API 或 build 通过被误当成全系统稳定。新的闸门把这两类偏差都压住了。

## 追加收口：CRM 导入与详情编辑类型边界

本轮只处理 CRM 主数据页面的低风险类型边界，不改变客户建档、客户池、三语名称、多地址、多联系人的交互设计。

三步法复核：

- PowerShell 对部分中文文件仍可能显示乱码，不能据此直接重写。
- 本轮实际处理的是 `pages/crm/useCRM.tsx` 和 `pages/crm/CRMCustomerDrawer.tsx` 的代码级 `any` 边界，不是源码中文损坏。
- 有效源码禁乱码闸门通过，确认没有新增乱码。

已完成：

- `pages/crm/useCRM.tsx` 新增 `ImportedCustomerRow`，批量导入从 `any[]` 改为受控的导入行结构。
- 联系人导入从直接 `as Contact[]` 改为 `unknown -> normalizeContactRecord -> Contact`，只有姓名、电话、邮箱至少一项有效时才进入客户主数据。
- 地址导入统一走 `parseImportedAddresses()` 与 `normalizeCustomerAddresses()`，保留旧 `address` 字段兜底。
- 客户统计中的 `overdueAmount`、`creditHold`、`shipmentHold` 直接使用 `Customer` 类型字段，不再通过 `(customer as any)`。
- 联系人和地址编辑函数改为字段泛型，避免详情页把错误类型写入 `Contact` 或 `CustomerAddress`。
- `pages/crm/CRMCustomerDrawer.tsx` 的 `t`、`onUpdateContact`、`onUpdateAddress` prop 不再暴露 `any`。

复核结果：

- `npm run build`：通过。
- `npm --prefix backend run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 37。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T03-40-47-924Z.json`。

反思：

- 这包方向没有偏离主线。它没有为了数字好看去重写 CRM 页面，而是先收紧导入、联系人、地址这些最容易造成“保存了但结构脏”的入口。
- 仍不能宣布“全系统完美”。当前证据证明核心发布闸门通过、CRM 这两个文件的 `any` 边界已清，但浏览器级全页面真实输入、长时运行、并发冲突和剩余 P2 类型边界还需要继续分包推进。

## 追加收口：客户后端访问与 payload 类型边界

本轮继续围绕客户主数据后端，不改变权限策略、不改变客户池状态机、不改变三语名称、多地址、多联系人的业务字段。

已完成：

- `backend/src/controllers/customer/customer.access.ts` 引入 `Prisma.CustomerWhereInput`，客户读取/导出范围 where 不再使用 `Record<string, any>`。
- `hasDataScope()` 参数改为 `DataScope`，避免后续拼错数据范围字符串还被类型系统放过。
- 客户私海归属判断改为 `CustomerAccessTarget`，只读取 `poolState` 和 `salespersonId` 两个必要字段。
- `backend/src/controllers/customer/customer.payload.ts` 新增小型安全读取 helper，将联系人、别名、三语名称、地址、订单统计和 payload 输出从 `any` 改为 `unknown -> helper -> 字段值`。
- `backend/src/controllers/customer/customer.persistence.ts` 的事务入参改为 `Prisma.TransactionClient`，地址持久化入参改为 `serializeCustomerAddresses()` 的明确输入类型，联系人持久化入参改为 `unknown` 后再序列化。

复核结果：

- 定点扫描：上述三份客户后端文件无代码级 `any/as any/Record<string, any>`。
- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 34。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T03-46-08-624Z.json`。

反思：

- 这包继续走对方向：先把客户链路的类型边界收紧，再考虑客户 controller 进一步拆分。这样比直接大拆文件更稳，因为先把输入/输出形状固定住了。
- 需要继续警惕：`schema.prisma`、客户/订单/采购/发货/生产/差异等大文件仍是维护债，但不应为了“行数下降”直接拆；应在业务闭环验证足够时按领域抽 helper、mapper、policy。

## 追加收口：RMA / 调账旧请求串页与稳定 runtime

本轮处理的是“代码已修但浏览器仍加载旧构建”的反幻觉问题。上一轮 `npm run verify:release:core` 失败点为 `cdp-core-pages-smoke` 的 `route-adjustment exceeded 9000ms`，浏览器报告显示 RMA 旧请求的 401/timeout 会串到后续页面；首次代码修复后 CDP 变为 `passed_with_warnings`，但 warning 栈仍指向旧的 `RMA-BoZFCXRD.js`，而当前 build 已生成新的 `RMA-BZlwCqvJ.js`，由此确认根因是 5001 稳定运行层仍在服务旧 dist。

已完成：

- `services/rma.service.ts` 增加 `ApiRequestOptions.signal`，并把 RMA 回包从 `any` 收束为 `unknown -> mapRmaRecord -> RmaRecord`。
- `pages/RMA.tsx` 增加 `AbortController`，离开页面时取消旧请求，取消错误不再写入 console。
- `services/adjustment.service.ts` 的 `getSummary()` 增加 `signal` 参数。
- `pages/adjustment/AdjustmentCenterView.tsx` 将 summary 改为 `AdjustmentSummary`，列表和汇总请求均支持取消，取消请求不触发错误 toast。
- 复核 5001 进程归属为爱牢达稳定链：`DATABASE_URL=file:D:/AilaoDaRuntime/stable.db`、`SERVE_FRONTEND=true`、`PORT=5001`、`node backend/dist/server.js`。
- 使用 `scripts/start-stable-v2.ps1` 安全重启 5001，脚本只停止爱牢达归属进程；未触碰智仔的 5173/518x/519x 进程。

复核结果：

- `npm run build`：通过。
- `npm --prefix backend run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 32。
- 重启前 `node scripts\cdp-core-pages-smoke-audit-v1.cjs`：`passed_with_warnings`，warning 为旧 RMA chunk 的 timeout，判定为旧 runtime 污染。
- 重启后 `node scripts\cdp-core-pages-smoke-audit-v1.cjs`：干净 `passed`，无 HTTP failure、无 console warning、无 fatal console error。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-05-13-767Z.json`。

反思：

- 本包没有偏离主线。真正的问题不是继续加业务功能，而是“有效代码、运行产物、浏览器实际加载内容”三者不一致，容易造成假失败或假通过。以后只要浏览器报告里的 chunk 名与最新 build 不一致，必须先判定为 stale runtime，不允许继续盲修业务代码。
- 仍不能宣布全系统完美。当前证据证明稳定入口 5001、核心 18 路由、AI 安全回归、前后端构建、lint 和有效源码禁乱码通过；剩余工作是继续压缩 P2 类型边界、大文件拆分债、真实输入长时/并发/恢复验收。

## 追加收口：后端调账服务事务与 where 类型边界

本轮继续处理后端 P2，但只碰调账服务的类型边界，不改变调账业务状态机。调账服务连接财务回款金额、客户逾期金额、生产批次数量和库存冲销，如果这里保留 `any`，后续线上排查会很难判断到底是金额口径错、批次数量错，还是调用方传错结构。

已完成：

- `backend/src/services/adjustment.service.ts` 引入 `TransactionClient`，`recalculateCustomerOverdueAmountTx()`、`applyToFinanceTx()`、`applyToProductionTx()`、`applyAdjustmentTx()` 不再使用 `tx:any`。
- 调账应用函数的 `adjustment:any` 改为 Prisma 生成的 `AdjustmentRecord` 类型，保留原有字段与状态判断。
- `listAdjustments()` 的外部追加 where 改为 `Prisma.AdjustmentRecordWhereInput`。
- `getSummary()` 的 where 入参改为 `Prisma.AdjustmentRecordWhereInput`。
- 中途反思发现长 `Pick<>` 类型块会把文件推入“大文件 P2”，已改为 `type PersistedAdjustment = AdjustmentRecord`，保留类型安全同时不制造额外行数债。

复核结果：

- 定点扫描：`backend/src/services/adjustment.service.ts` 无代码级 `any/as any/Record<string, any>`。
- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 31。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-09-22-684Z.json`。

反思：

- 这包方向正确，但有一个很重要的自纠：类型安全不能靠堆超长类型块完成，否则会把“类型债”转换成“维护行数债”。后续收 P2 时优先复用 Prisma 模型类型、已存在的 `TransactionClient` 和小 mapper，不为了审计数字写冗长样板。
- 调账服务现在更稳，但仍没有完成调账模块的全业务深测。当前证据覆盖发布核心、页面烟测、构建和 AI 安全；后续还需要对“调账创建 -> 生效 -> 金额/数量变化 -> 冲销 -> 回读”的专项业务闭环做 API 和浏览器双证据。

## 追加收口：订单工作台详情/导入/导出类型边界

本轮处理订单工作台服务。订单是 CRM、回款、发货、货抵、合同和经营分析的大闭环上游，历史上已经出现过“列表有客户名、详情没有客户名”的问题，所以这里不继续容忍服务层 `any`。

已完成：

- `ORDER_DETAIL_INCLUDE` 改为 `satisfies Prisma.OrderInclude`，`formatOrderDetail()` 入参改为 `Prisma.OrderGetPayload<{ include: typeof ORDER_DETAIL_INCLUDE }>`，订单详情不再靠 `any` 展平客户名、创建人、明细和回款记录。
- 订单列表筛选 `filters` 改为 `Prisma.OrderWhereInput`。
- 批量导入 `importOrders()` 入参从 `orders:any[]` 改为 `unknown`，通过 `toImportOrders()` 归一为 `ImportOrderRow[]` 后再校验客户 ID、明细行和账期。
- 导出筛选 `filterWhere` 改为 `Prisma.OrderWhereInput`。
- 导出合同号不再使用 `(o as any).contract`，改为直接读取 `o.contract?.contractNo`。
- 类型化过程中暴露 `createdAt` 日期范围不能直接 spread 的真实隐患，已改为显式 `Prisma.DateTimeFilter`，避免开始时间/结束时间组合时被动态对象吞掉类型错误。
- 文件保持 499 行，没有把 high-any 债转换成 oversized file 债。

复核结果：

- 定点扫描：`backend/src/services/order-workspace.service.ts` 无代码级 `any/as any/Record<string, any>`。
- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 30。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-13-53-597Z.json`。

反思：

- 这包的收益不只是 P2 数字下降，而是订单详情、批量导入和导出三个最容易产生“前端看着能用、实际字段脏”的入口都被类型系统管住了。
- 仍不能把订单模块宣布为完整商业级闭环。当前证据覆盖 release core 的订单回读与回款核验，但还需要后续把订单导入 Excel 真实样本、订单头表单 + 明细 Excel 网格、订单三轴状态机专项浏览器流继续补证据。

## 追加收口：生产完工/BOM 消耗校验类型边界

本轮处理生产 mutation 服务的 high-any。这里是化工生产链的核心：BOM 原料覆盖、替代料、单位匹配、消耗上下限、完工入库、原料扣减和成本台账都经过它。因此本包只做类型边界，不改任何 10 原料 BOM 校验规则和业务顺序。

已完成：

- `resolveFinishedGoodsLocationId()` 的事务 client 从 `any` 改为 `TransactionClient`。
- 新增 `BomValidationItem` 和 `StockValidationSnapshot`，BOM 校验项、库存快照、替代料组和消耗记录不再使用 `any`。
- `getMaterialLabel()`、`getToleranceRateForBomItem()`、`bomItemMatchesStock()`、`assertBomConsumptionCoverage()` 全部改为显式类型。
- 工序创建时兼容旧 `name` 字段，但不再使用 `(step as any)`。
- 工单状态更新 `updateData` 从 `Record<string, any>` 改为 `Prisma.ProductionWorkOrderUpdateInput`。
- 类型化过程中暴露 BOM `unit` 可能为 `null`，而结构化 issue 只接受 `string | undefined`；已把 issue 字段改为 `item.unit || undefined`，不改变用户可读 message。

复核结果：

- 定点扫描：`backend/src/services/production-mutation.service.ts` 无代码级 `any/as any/Record<string, any>`。
- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 29。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-18-25-579Z.json`。

反思：

- 这包没有偏离化工厂主线。它没有为了“漂亮重构”去拆生产大文件，而是先把最容易导致完工假通过、原料扣减错账的类型边界收住。
- 仍需补专项业务证据：10 行化工 BOM 的真实验收要继续保留“缺 1 种原料必须阻止、补齐后必须完工并生成原料消耗和成品入库凭证”的 API/浏览器双闭环。这个标准不是形式主义，而是为了防止工单假完工、成本假准确。

## 追加收口：收发货差异 raw SQL row 与事务类型边界

本轮处理收发货差异服务。该服务连接采购拒收、客户短签、容差规则、差异动作和结案，是“异常不糊账”的关键层。服务内部大量使用 raw SQL，本包不改 SQL 语义，只把原始行和事务边界收紧。

已完成：

- 新增 `RawRow = Record<string, unknown>`，替代所有 `Record<string, any>` raw SQL 行。
- 新增 `toNullableNumber()`、`toNullableString()`、`toRowString()`、`toRowBoolean()`、`normalizeEnumValue()`，统一映射 case、tolerance rule、action 三类结果。
- `normalizeCaseRow()`、`normalizeRuleRow()`、`normalizeActionRow()` 不再直接读取动态 `any` 字段。
- `resolveToleranceDecision()`、`createCaseForRejectedReceipt()`、`listToleranceRules()`、`createToleranceRule()`、`listActions()`、`createAction()`、`listCases()`、`resolveCase()` 的 tx 入参改为 `TransactionClient`。
- `$queryRawUnsafe` 调用改为 `RawRow[]` 泛型返回，去掉后置 `as Array<Record<string, any>>`。
- 保留原有 SQL、容差决策、动作防重、解决状态和业务顺序。

复核结果：

- 定点扫描：`backend/src/services/receipt-discrepancy.service.ts` 无代码级 `any/as any/Record<string, any>`。
- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 28。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-22-26-423Z.json`。

反思：

- 这包最关键的价值是 raw SQL 不再是“黑盒动态对象”。以后如果差异单出现字段缺失、状态错、容差误判，可以从 `RawRow -> mapper -> 业务对象` 这条链定位，而不是在任意 `any` 上猜。
- 仍需补专项验收：采购拒收生成差异、客户短签生成差异、容差内自动允许/警告、容差外阻断/人工复核、差异动作防重复、结案回读都还应该有专门的 API 与浏览器证据。

## 追加收口：货抵/资产批次类型边界

本轮处理最后一批 high-any：货抵后端服务、货抵控制器和资产批次控制器。这里覆盖两条容易上线后炸账的链路：货抵支付/换货贸易的分批抵扣，以及产品批次的保质期、库存数量和冷链标记。

已完成：

- `backend/src/services/barter.service.ts`：
  - `syncAgreementProgress()` 的 client 从 `any` 改为统一 `TransactionClient`。
  - 协议执行金额 reduce 不再使用 `settlement:any` 和 `posting:any`。
  - `getSummary()` where 改为 `Prisma.BarterSettlementWhereInput`。
  - 协议列表 where 改为 `Prisma.BarterAgreementWhereInput`，执行批次列表 where 改为 `Prisma.BarterSettlementWhereInput`。
  - 列表结果直接使用 Prisma 推导类型，不再 `(items as any[]).map(...)`。
- `backend/src/services/barter/barter.types.ts`：
  - 货抵查询入参的 `where` 从 `Record<string, any>` 改为对应 Prisma where input。
- `backend/src/services/barter/barter.formatters.ts`：
  - `parseBarterMetadata()` 改为明确的 settlement/item metadata 结构，不再让回款 metadata 由 `any` 承载。
- `backend/src/controllers/barter.controller.ts`：
  - `validateSalesBarterPayload()` 入参改为只读取需要的未知字段。
  - 创建货抵单、货抵协议、执行批次的审计日志直接读取服务返回字段，不再通过 `(data as any)` 猜字段。
- `backend/src/controllers/asset.controller.ts`：
  - 产品批次列表、新建、更新、删除从 `(prisma as any).productBatch` 回到正式 `prisma.productBatch`。
  - 批次更新数据改为 `Prisma.ProductBatchUpdateInput`。
  - 批次请求体先进入 `unknown` 边界，再转换为字符串、日期、数字和布尔值。

复核结果：

- 定点扫描：`barter.service.ts`、`barter.controller.ts`、`barter.formatters.ts`、`barter.types.ts`、`asset.controller.ts` 均无代码级 `any/as any/Record<string, any>`。
- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，372 个有效源码文件未发现乱码。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 25。
- 剩余 P2 已确认全部为 oversized source file 拆分维护债，不再是 high-any 类型穿透。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-31-23-919Z.json`。

反思：

- 这包没有偏离主线。它没有把货抵交易或批次库存重写成新系统，只是把最后一批会绕过编译器的动态边界收住。
- 当前不能因为 release core 通过就说“完美上线”。更诚实的结论是：核心运行闸门通过、P0/P1 清零、高风险类型穿透清零；下一层主要是 25 个大文件拆分维护债，以及按业务专项继续补真实浏览器输入、长时并发、备份恢复、EXE 打包长跑证据。

## 追加收口：本地 AI 表格识别拆分与旧请求 401 防踢登录

本轮开始处理 oversized P2。优先选择 `services/freeAIService.ts`，因为它只有 669 行，且表格识别逻辑是纯前端本地规则层，适合作为低风险拆分样板。

已完成：

- 新增 `services/freeAI/tableRecognition.ts`：
  - 承接表格类型识别。
  - 承接客户、订单、样品、物流、通用表格解析。
  - 承接字段映射、数字解析、上下文汇总辅助。
  - 导出 `ParsedFormType`、`ParsedFormRecord`、`ContextData`、`ParsedFormData`。
- `services/freeAIService.ts`：
  - 保留 `parseTableFile`、`recognizeAndParseTable` 对外导出不变。
  - 保留 AI 命令回复、风险洞察、合同/标签本地占位分析。
  - 文件从 669 行降至 282 行。
- `utils/api.ts`：
  - release core 首次复核时，CDP 在 `#discrepancies` 页面被踢回登录页。
  - 失败证据显示触发点为 `shipping` 离页旧请求 `/api/assets/balance` 返回 401，旧响应拦截器把“无 requestToken 的 401”也当成当前登录失效，清空了登录态。
  - 已收紧 401 判断：只有请求携带当前 token 被后端拒绝，或当前本来就没有会话，才清理 token 并跳登录；无 requestToken 但当前有会话的旧请求只作为普通错误上抛，不再踢当前用户。

复核结果：

- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，373 个有效源码文件未发现乱码。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 24。
- `npm run verify:release:core` 第一次失败：CDP `#discrepancies` 超时，截图显示已回登录页，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-36-57-356Z.json`。
- 修复 401 防踢后，`npm run verify:release:core` 第二次 9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-40-40-534Z.json`。

反思：

- 这包证明“拆分大文件”也可能暴露真实运行细节。表面上只是拆 AI 表格识别，实际 release core 抓到的是跨页旧请求 401 误踢登录，这正是人工点测里最容易偶发、最难复现的问题。
- 后续拆分必须继续保留 release core 作为硬闸门，不能只看 TypeScript 或 Vite build。

## 追加收口：后端校验层客户/生产域拆分

本轮处理 `backend/src/validators/index.ts`。这是典型“所有输入规则堆一处”的维护债：后续排查客户三语名称、多地址、多联系人、生产 BOM、调账规则时，很容易在一个近千行文件里找不到边界。

已完成：

- 新增 `backend/src/validators/customer.ts`：
  - 客户主数据基础字段。
  - 多地址实体校验。
  - 多联系人实体校验。
  - 客户创建、更新、公海/私海池更新、联系人 schema。
- 新增 `backend/src/validators/production.ts`：
  - 调账创建校验。
  - 化工 BOM 物料行校验。
  - BOM 主数据校验。
  - 工单创建、工单状态更新、工序更新、质检记录校验。
- `backend/src/validators/index.ts`：
  - 保留兼容出口，继续从 `./customer` 和 `./production` re-export 原 schema 名称。
  - 所有历史 `import { createCustomerSchema } from '../validators'` 类型路径无需改动。
  - 文件从 928 行降至 590 行，低于当前 oversized 闸门阈值。

复核结果：

- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，375 个有效源码文件未发现乱码。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 23。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-46-25-865Z.json`。

反思：

- 这包拆的是输入契约，不是业务执行链。风险低但长期收益高，因为客户和生产两个最复杂业务域从此有独立校验入口。
- 下一步可以继续拆服务/页面大文件，但不能为了 P2 数字贸然拆采购、生产、CRM 这种复杂页面。更稳的顺序是先拆 hook/helper/service 层，再动 UI 组件。

## 追加收口：回款中心加载闭环拆分

本轮处理 `pages/collections/useCollectionCenter.ts`。这块之前已经做过聚合读模型和旧请求治理，因此本轮只抽“加载小闭环”，不碰 UI、不碰权限、不碰动作逻辑。

已完成：

- 新增 `pages/collections/collectionLoadBundle.ts`：
  - `CollectionLoadOptions`。
  - `requestCollectionLoadBundle()`。
  - 聚合接口 `collectionsService.getWorkbench()` 优先加载。
  - 聚合失败后回退到 summary、ledger、overdue、milestones、promises、disputes、holds 七接口。
  - 按 token key 做 inflight 去重，避免 StrictMode 或快速切页重复请求。
  - 保留 `.finally().catch()` 防止 cleanup 分支变成未捕获 pageerror。
- `pages/collections/useCollectionCenter.ts`：
  - 保留原状态、权限、筛选、排序、催收、核销、承诺付款、争议、拦截释放逻辑。
  - 主 hook 从 636 行降至 540 行。

复核结果：

- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，376 个有效源码文件未发现乱码。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 22。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T04-50-03-779Z.json`。

反思：

- 这包是“先拆 hook/helper，不拆页面视觉”的正确方向。回款中心最容易出问题的是加载和旧请求，本包把这条链独立出来，后续维护成本下降。
- 仍需后续专项：回款中心真实批量催收、承诺付款多次回读、争议状态切换、客户/订单拦截释放，应该保留浏览器级证据，而不是只依赖 release core。
## 追加收口：收发货差异工作台配置拆分与风控聚合读模型

本轮继续处理 oversized P2，但只动边界清晰的小包，不重写页面、不改变视觉风格、不改变收发货差异业务动作。

已完成：

- `pages/ReceiptDiscrepancyWorkbench.tsx`
  - 将 `ActiveTab`、`RuleDraft`、差异来源/类型/动作/状态/责任方标签、选项数组、数字格式和搜索文本构造拆到 `pages/receiptDiscrepancyWorkbench.config.ts`。
  - 主页面从 606 行降至 589 行，配置文件 125 行。
  - 未改变采购拒收、客户短签、容差规则、差异动作、规则保存、页面布局和 UI 样式。
- `pages/RiskControl.tsx`
  - release core 首次复核时，`cdp-core-pages-smoke` 在 `#risk` 页面捕获到 `pageerror: timeout of 10000ms exceeded`。
  - 三步法判断后确认：不是源码乱码、不是端口误入智仔、不是 Risk 页面稳定崩溃；单页和同序列探针均未复现，风险点更接近快速切页时回款/风控分散请求偶发超时。
  - 将风控页从 `getSummary()`、`getOverdueOrders()`、`getLedger()` 三条并发读，收敛为已治理过的 `collectionsService.getWorkbench()` 聚合读模型，一次回读 summary/overdue/ledger，减少旧请求串页和超时面。

复核结果：

- `npm run build`：通过。
- `npm --prefix backend run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，扫描 377 个有效源码文件，0 发现。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 21。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T05-00-00-497Z.json`。

反思：

- 本包没有偏离主线。第一步是大文件维护债拆分，第二步是 release core 暴露真实快速切页稳定性问题后的最小修复。
- 不应把第一次 release core 失败忽略为偶发；它提示风控页仍在使用旧的三接口分散加载方式，和回款中心已经完成的聚合读治理不一致。
- 当前证据只能说明核心发布闸门通过、P2 降至 21、风险页快速切页超时已被聚合读模型压住；仍不能宣布全系统完美。后续继续按 P2 大文件拆分、真实浏览器输入回读、并发对账、备份恢复、EXE 长跑证据推进。

## 追加收口：销售订单 hook 拆分与资产旧请求取消链

本轮继续处理 P2 大文件维护债，同时不扩大业务改动面。优先选择销售订单 hook，是因为它已经有独立页面与 helper 边界，适合先拆表单、导入、OCR、扫码这些纯前端计算逻辑；不动订单 API、回款、客户范围和页面视觉。

已完成：

- 新增 `pages/sales-orders/salesOrderFormHelpers.ts`：
  - `SalesOrderFormData`、`PaymentForm`、`initialOrderForm`。
  - 订单金额、未收款、逾期天数、回款视图。
  - Excel 明细解析、导入 payload 构造。
  - 价格建议、库存洞察、智能填充、OCR 结果应用。
  - 扫码产品转明细、离线扫码缓存、明细合并。
- `pages/sales-orders/useSalesOrders.ts`：
  - 主 hook 从 930 行降至 599 行。
  - 保留原来的订单列表、客户列表、批次读取、创建/更新、回款、导入导出和弹窗状态。
  - 未改变订单三轴状态、回款核验、客户名称回读、库存批次提示和 UI 风格。
- `pages/useAssets.tsx`：
  - release core 首次复核时，`cdp-core-pages-smoke` 捕获到 `#assets` 离页后的 `/api/assets/history` 401 串到 `#adjustment`，导致路由超时。
  - 三步法判断后确认：报告里的“认证令牌无效或已过期”是有效 UTF-8 文案，不是源码乱码；真实问题是资产页读取没有 `AbortController`，旧请求可在离页后继续失败并污染后续页面。
  - 为资产余额、资产历史、批次读取补齐 `AbortController`、`isCanceledApiError()`、卸载后不回写和非取消错误提示。

复核结果：

- `npm run build`：通过。
- `npm --prefix backend run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，扫描 378 个有效源码文件，0 发现。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 20。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T05-19-10-761Z.json`。

反思：

- 方向没有偏离：这包先拆维护债，再处理发布验收暴露的真实路由串页问题，没有重写页面，也没有改订单/资产业务口径。
- 不能因为 API 和 release core 通过就宣称“全系统完美”。当前证据覆盖核心路由、构建、lint、乱码闸门和全仓审计；还需要继续推进剩余 P2 大文件、真实浏览器输入回读、并发/幂等/对账、备份恢复和 EXE 长时运行。
- 资产页这次暴露的问题说明：凡是首屏会发请求的页面，后续拆分时必须同步检查“可取消、可降噪、卸载后不回写”，否则快速切页还是会制造假崩溃。

## 追加收口：回款服务类型边界拆分

本轮继续按“低风险拆维护债”的顺序处理 `backend/src/services/collection.service.ts`。选择它的原因是：文件 643 行，已经接近阈值；顶部堆放大量类型与客户展示名 helper，适合拆出，不需要改回款业务执行链。

已完成：

- 新增 `backend/src/services/collection/collection.types.ts`：
  - `getCustomerDisplayName()`。
  - `PaymentStateInput`。
  - `AgingBucketSummary`。
  - `ReceivablesSnapshot` 与 `ReceivablesBaseSnapshot`。
  - `CollectionPromiseRecord`、`CollectionDisputeRecord`、`CollectionHoldRecord`。
- `backend/src/services/collection.service.ts`：
  - 从本地类型定义改为从 `collection.types.ts` 导入并 re-export，保留旧 import 兼容能力。
  - 主文件从 643 行降至 525 行。
  - 未改变回款汇总、逾期分桶、优先催收动作、最近回款、承诺付款、争议、冻结/释放、审计日志写入。

复核结果：

- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，扫描 379 个有效源码文件，0 发现。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 19。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T05-25-03-351Z.json`。

反思：

- 方向正确：拆的是“类型和展示名规则”，不是回款状态机，不会引入新的业务分支。
- 这包说明接下来可以继续优先找“文件略超阈值、边界清晰”的模块拆，而不是贸然重构 `ProductionWorkspaceV2.tsx`、`Procurement.tsx` 这种高耦合页面。
- 后续回款仍需要更深的浏览器真实输入证据：多笔承诺付款、多笔核销、争议状态流转、冻结释放后的订单/客户回读。

## 追加收口：生产完工 BOM 消耗校验拆分

本轮处理 `backend/src/services/production-mutation.service.ts`。这块属于高价值业务链，因此只拆“完工前校验器”，不改变工单状态机、库存扣减、成品入库、成本台账执行顺序。

已完成：

- 新增 `backend/src/services/production-completion.validation.ts`：
  - `ProductionCompletionValidationError` 与结构化 `issues`。
  - BOM 消耗覆盖校验。
  - 原料编码/名称与库存批次匹配。
  - 单位一致性校验。
  - 按化工原料角色的用量容差：树脂、固化剂、催化剂、助剂、填料、颜料、溶剂、包装材料等。
  - 替代料组覆盖校验。
- `backend/src/services/production-mutation.service.ts`：
  - 保留 `ProductionCompletionValidationError` 从原路径 re-export，兼容 `production.controller.ts` 的现有 import。
  - 主服务从 729 行降至 493 行。
  - 补回主流程仍需要的 `toPositiveNumber()`，避免把共享小工具误删。
  - 未改变 `assertBomConsumptionCoverage()` 的调用点、完工前库存校验、库存扣减、成品入库、成本台账和状态更新时间。

复核结果：

- `npm --prefix backend run build`：通过。
- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，扫描 380 个有效源码文件，0 发现。
- `node scripts\full-codebase-audit-v1.cjs`：通过，P0/P1/P3 为 0，仅剩 P2 18。
- `npm run verify:release:core`：9/9 通过，0 failed，0 stuck，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-19T05-30-23-239Z.json`。
- `node scripts\chemical-bom-production-chain-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\chemical-bom-production-chain-audit-report-v1.json`；真实证据为 10 行化工 BOM 只耗用 1 种原料时被阻止并返回 9 个缺料问题，补齐后生成 1 条原料消耗、1 条成品入库、1 条成本台账。

反思：

- 方向没有偏离。化工 BOM 是核心主线，拆校验器比拆 UI 大页面更稳，因为它把“完工能不能过”的核心规则独立出来，后续更容易单测和复核。
- 这次构建先失败一次，暴露 `toPositiveNumber()` 在主流程仍被使用；已经按最小修复补回，没有扩大改动面。
- 后续更值钱的生产专项不是继续盲拆，而是补一条真实业务回归：10 行化工 BOM，少耗用 1 种必须被阻止，补齐后必须生成原料扣减、成品入库和成本台账。
## 追加收口：2026-04-20 核心业务浏览器验收与并发对账

本轮目标不是新增功能，而是按“证据链优先”复核当前稳定入口是否仍然是爱牢达系统，并把浏览器真实点测、后端链路、并发对账和发布闸门串成一套可追溯证据。

运行边界：

- 稳定入口固定为 `http://127.0.0.1:5001/`。
- 本轮未使用 `5173/518x` 作为爱牢达入口，避免与智仔运行口混用。
- 运行时数据库为 `D:\AilaoDaRuntime\stable.db`。
- 所有长运行命令按 300 秒上限执行，浏览器自动化因当前环境 `spawn EPERM` 时按权限流程重跑。

本轮通过证据：

- `npm run check:runtime`：通过，5001 的 health、home、manifest、icon、sw、assets 均可访问。
- `node scripts\dual-port-audit-v2.cjs`：20/20 通过，稳定模式只验证 5001。
- `node scripts\backend-business-chain-audit-v1.cjs`：通过，覆盖登录、仓库/库位、供应商、客户、订单、确认、回款、采购、收货、差异、库存、发货、签收回执和订单回读。
- `node scripts\crm-masterdata-browser-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\crm-masterdata-browser-audit-report-v1.json`。
- `node scripts\procurement-browser-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\procurement-audit-report-v1.json`。
- `node scripts\sales-orders-browser-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\sales-orders-audit-report-v1.json`。
- `node scripts\collections-browser-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\collections-audit-report-v1.json`。
- `node scripts\barter-agreement-browser-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\barter-agreement-audit-report-v1.json`。
- `node scripts\production-browser-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\production-browser-audit-report-v1.json`。
- `node scripts\shipping-browser-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\shipping-audit-report-v1.json`。
- `node scripts\adjustment-browser-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\adjustment-audit-report-v1.json`。
- `node scripts\backup-restore-api-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\backup-restore-api-audit-report-v1.json`。
- `node scripts\concurrency-reconcile-deep-audit-v1.cjs`：通过，3 个冲突场景、0 个对账漂移，报告为 `F:\爱牢达\output\playwright\concurrency-reconcile-deep-audit-report-v1.json`。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，扫描 380 个当前有效源码文件，0 个发现。
- `node scripts\full-codebase-audit-v1.cjs`：完成，P0/P1/P3 为 0，仅剩 P2 18。
- `npm run verify:release:core`：9/9 通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-06-23-642Z.json`。

三步法判断：

- 生产浏览器验收首轮失败点为 `quality check not found in API readback`。
- 翻译与还原后确认这不是源码乱码，也不是生产业务代码未保存质检；手工 API 读回同一工单时能看到对应 `qualityChecks` 记录。
- 根因是验收脚本在点击保存质检后立刻读 API，没有等待后端读模型回写；已将 `verify-quality-check-api-readback` 改为按质检备注轮询读取，属于验收脚本假失败修复，未改变生产业务状态机。

并发脚本判断：

- 并发对账首轮出现 `Customer not found`。
- 使用同一 sales 账号做最小诊断：创建客户、按 ID 读回客户、按该客户创建订单均通过。
- 完整并发脚本复跑通过，覆盖订单重复回款、货抵批次/过账重复提交、发货出库/签收回执重复提交，未发现重复入账、重复扣库或状态漂移。

反思：

- 方向没有偏离。本轮没有继续盲目加功能，而是把“真实入口、真实浏览器、真实 API、真实对账、真实发布闸门”串起来。
- 不能因为本轮通过就宣称系统完美。本轮证明核心稳定入口和主要业务闭环可运行，并且当前有效源码、构建、lint、AI 安全、核心页面和并发对账均过闸；剩余 P2 仍是维护成本债，后续应继续按低风险拆分和专项浏览器深测推进。
- 后续所有浏览器脚本如遇“保存后读不到”，必须先判断是否为读模型等待不足，禁止直接改业务代码。

## 追加收口：并发对账脚本维护债拆分

本轮在核心业务验收通过后，继续处理低风险 P2 维护债。没有拆后端业务状态机，也没有碰生产、订单、发货、货抵的业务代码，只拆并发对账审计脚本的 fixture 和 API client。

改动内容：

- 新增 `scripts/lib/concurrency-reconcile-audit-utils.cjs`。
- 将并发对账脚本中的测试数据构造、接口请求、登录、客户/订单创建、库存种子、货抵执行、发货回执等通用辅助函数移入工具文件。
- `scripts/concurrency-reconcile-deep-audit-v1.cjs` 保留场景编排和断言逻辑，主文件从 768 行降至 570 行。
- 修复拆分后遗漏的 `unwrapList` 调用，避免工具函数搬迁后产生 lint 假绿。

复核结果：

- `node scripts\concurrency-reconcile-deep-audit-v1.cjs`：通过，3 个冲突场景、0 个对账漂移，报告为 `F:\爱牢达\output\playwright\concurrency-reconcile-deep-audit-report-v1.json`。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：完成，P0/P1/P3 为 0，P2 从 18 降至 17。
- `npm run verify:release:core`：9/9 通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-14-08-635Z.json`。

反思：

- 这包方向正确：先拆验收脚本维护债，降低后续并发/幂等/对账测试的维护成本，不冒然拆客户 controller 或生产大页面。
- 中途 lint 抓到 `unwrapList` 漏迁移，说明“拆分后必须跑 lint + 专项脚本”是必要制度，不允许凭肉眼判断通过。
- 下一包仍应优先选择低耦合 P2；客户 controller、采购页、生产页属于高价值但高耦合文件，必须等边界拆清楚后再下刀。

## 追加收口：旧 timber 接口断开与 410 标记

本轮处理“不能接的旧接口”。扫描后确认 `/api/timber/*` 仍被后端挂载，但当前业务主线已经从早期“木材抵扣”演进为通用“货抵支付 / 换货贸易”，前端有效入口也已归一到货抵模块。继续让 timber 接口返回旧汇总或旧换算，会制造隐藏双口径，因此本轮不删除文件，而是按旧接口隔离制度明确断开。

已完成：

- `backend/src/routes/timber.routes.ts` 改为旧接口禁用壳，所有 `/api/timber/*` 请求统一返回 `410 Gone`。
- 禁用响应包含 `code=LEGACY_TIMBER_API_DISABLED`、`replacement=/api/barter`、`disabledAt=2026-04-20`，让调用方知道不是服务器异常，而是旧接口已废弃。
- `backend/src/controllers/timber.controller.ts` 保留为历史参考，并在文件头标记 `LEGACY DISCONNECTED`，避免后续误认为它仍是正式入口。
- `scripts/role-read-permission-audit-v1.cjs` 将 timber summary 从权限矩阵中的可读项改为“全角色均应 410”的禁用项。
- 新增 `scripts/legacy-interface-disconnect-audit-v1.cjs`，覆盖 `calculate`、`summary`、`convert-po` 的匿名和角色访问，验证均返回 410 且 replacement 指向 `/api/barter`。
- `结构目录与运行链_2026-04-18.md` 已将 `/api/timber` 标记为旧木材专用接口，当前替代入口为 `/api/barter`。

复核结果：

- `npm --prefix backend run build`：通过。
- `npm run check:runtime`：通过。
- `node scripts\legacy-interface-disconnect-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\legacy-interface-disconnect-audit-report-v1.json`。
- `node scripts\role-read-permission-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\role-read-permission-audit-report-v1.json`。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：完成，P0/P1/P3 为 0，仅剩 P2 17。
- `npm run verify:release:core`：9/9 通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-23-10-936Z.json`。

反思：

- 这次没有删除旧文件是正确的。旧接口断开需要可追溯、可审计、可被调用方识别，直接删除反而会让未来排查失去上下文。
- 410 比 404 更合适，因为它明确表达“这个接口曾经存在，但已经被治理性废弃”，不会让测试员误判为路由漏挂或环境坏了。
- 后续发现旧接口时统一采用同一制度：先确认是否仍被正式入口引用，再改为 410 禁用壳，补 replacement，补专项审计，补运行链登记；禁止只在前端隐藏按钮但后端继续可用。

## 追加收口：旧 timber 前端入口与旧 mock 审计口径

本轮继续追查“旧接口虽已 410，但前端或审计层是否还会误接回去”。确认结果如下：

- 正式壳层 `useAppShell.tsx` 会将 `#timber` 归一为 `#barter`。
- 正式页面注册表 `activePathRegistry.ts` 不再挂载旧 `TimberWorkspace`，货抵正式页面为 `BarterWorkspaceView`。
- `pages/TimberWorkspace.tsx` 原本仍残留 `/api/timber/calculate`、`/api/timber/summary` 旧请求；本轮已降级为兼容 import shell，误导入时直接进入 `BarterWorkspaceView`，不再触发旧接口。
- `backend/src/controllers/timber.controller.ts` 已从历史计算、summary 查询、转采购实现降级为 410 禁用控制器，避免未来有人误把 controller 重新接回 route 后旧逻辑复活。
- `backend/src/utils/TimberEngine.ts` 与旧 timber validator schema 已标记 `LEGACY DISCONNECTED`，保留历史上下文但不作为正式接口接入依据。
- 英文、越南文、中文的 `timber` 主口径已从木材/板材修正为货抵支付/换货贸易，避免语言切换后又把旧业务名称显示给用户。
- `scripts/full-codebase-audit-v1.cjs` 已补充识别 `backend/simple-server.js` 为已禁用旧 mock；目前 `backend/complete-server.js` 和 `backend/simple-server.js` 均属于 disabled legacy，不会污染活跃旧入口风险判断。

复核结果：

- `npm run build`：通过。
- `npm run lint`：通过。
- `node scripts\legacy-interface-disconnect-audit-v1.cjs`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过，扫描 382 个有效源码文件，0 发现。
- `node scripts\full-codebase-audit-v1.cjs`：完成，P0/P1/P3 为 0，仅剩 P2 17；`disabledLegacyFiles=2`。
- `npm run check:runtime`：通过，5001 的 health、home、manifest、icon、service worker 和当前 dist assets 均可访问。
- `npm run verify:release:core`：9/9 通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-37-49-647Z.json`。

反思：

- 这包没有继续扩大功能，而是把“旧接口不能接”的链路从后端延伸到前端和审计层，方向正确。
- 旧页面不应删除，因为历史上下文仍有价值；但旧页面也不能保留真实旧 API 调用，所以兼容壳比删除或继续保留旧表单都更稳。
- 多语言口径属于容易被忽略的“假修复”：中文正确但英文/越南文仍显示木材，会让真实用户以为旧业务还存在。本轮已补掉主口径，但旧 timber 计算细项翻译键仍保留为历史键，后续如完全不再引用，可进入翻译键清理包。

## 追加收口：旧入口判定表与误运行入口禁用

本轮用于纠正上一包可能出现的范围扩张：不再凭 `legacy`、`old`、`fix`、`timber` 等关键词直接改代码，而是先建立判定表，再只处理会误接运行链、误写源码或误写数据库的入口。

新增文档：

- `爱劳达软件治理中心/10_主线任务/旧入口判定表_2026-04-20.md`

已完成：

- `refactor.cjs`、`refactor2.cjs`：加禁用壳，防止误运行后批量改写浏览器审计脚本。
- `backend/scripts/fix-db.js`、`backend/scripts/fix-db.ts`：加禁用壳，提示使用正式 `npm run db:manage -- repair`，避免绕过当前数据库修复入口直接写稳定库。
- `scripts/full-codebase-audit-v1.cjs`：将上述四个入口识别为 `disabledLegacyFiles`，不再污染活跃旧命名风险。
- 明确 `legacyAddress` 等旧数据兼容字段不断开，因为它们服务于旧客户数据读回，不是旧运行入口。
- 明确 7 组前后端同名文件不是旧接口，不在本包处理，后续如需治理应走命名清晰度专项。

复核结果：

- `node refactor.cjs`：快速退出，提示 legacy one-off refactor disabled。
- `node refactor2.cjs`：快速退出，提示 legacy one-off refactor disabled。
- `node backend\scripts\fix-db.js`：快速退出，提示 legacy db repair disabled。
- `npx tsx backend\scripts\fix-db.ts`：提升权限复核后快速退出，提示 legacy db repair disabled。
- `npm --prefix backend run build`：通过。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：完成，P0/P1/P3 为 0，仅剩 P2 17；`legacyNamedFiles=1`，`disabledLegacyFiles=6`。
- `npm run check:runtime`：通过。
- `npm run verify:release:core`：9/9 通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-46-03-927Z.json`。

反思：

- 本轮边界已经收窄回“旧入口治理”，没有继续扩散到普通 UI 或业务字段。
- `fix-db.*` 曾经作为维护工具保留是合理的，但当前已有正式 `db:manage -- repair`，继续允许旧脚本直接写 `stable.db` 风险更高，因此禁用旧入口而不删除历史代码是更稳的选择。
- 剩余 `legacyNamedFiles=1` 是 `legacy-interface-disconnect-audit-v1.cjs`，它是当前治理审计脚本，不应清理。

## 追加收口：化工 BOM 生产链审计脚本拆分

本轮继续处理低耦合 P2 维护债，选择 `scripts/chemical-bom-production-chain-audit-v1.cjs`，原因是它是审计脚本而非业务运行代码，且刚好超过 600 行阈值，适合先拆报告/API 辅助层，不动生产业务状态机。

已完成：

- 新增 `scripts/lib/chemical-bom-audit-utils.cjs`。
- 抽出报告写入、步骤记录、超时请求、登录、仓库/库位、库存余额、库存凭证、BOM、工单、预览消耗、成本台账、生产摘要等 API 辅助。
- `scripts/chemical-bom-production-chain-audit-v1.cjs` 主文件只保留化工 BOM 场景编排和断言，从 634 行降至 390 行。
- 保留原核心验收语义：10 种原料建 BOM；只耗用 1 种原料必须被后端阻止；补齐 10 种后必须完工，并读回原料扣减、成品入库、库存凭证和成本台账。
- 禁乱码闸门首次复核捕获主脚本测试数据中的源码层乱码，不属于终端显示层；已按三步法改成 ASCII 内部代号测试数据，避免测试脚本继续制造假乱码。

复核结果：

- `node --check scripts\chemical-bom-production-chain-audit-v1.cjs`：通过。
- `node --check scripts\lib\chemical-bom-audit-utils.cjs`：通过。
- `node scripts\chemical-bom-production-chain-audit-v1.cjs`：通过，报告为 `F:\爱牢达\output\playwright\chemical-bom-production-chain-audit-report-v1.json`。
- `npm run lint`：通过。
- `node scripts\effective-source-mojibake-gate-v1.cjs`：通过。
- `node scripts\full-codebase-audit-v1.cjs`：完成，P0/P1/P3 为 0，仅剩 P2 16。
- `npm run verify:release:core`：9/9 通过，报告为 `F:\爱牢达\output\audit\release-verification-v1-core-2026-04-20T13-55-54-019Z.json`。

反思：

- 这包方向正确：拆的是低耦合审计脚本，业务代码没有变化。
- 中途禁乱码闸门抓到测试数据乱码，说明三步法和闸门必须保持默认开启；如果只看专项通过，就会把乱码重新带进有效源码。
- 下一包仍应优先处理低耦合 P2，例如浏览器人工流审计脚本或后端业务链审计脚本；不建议马上拆 `ProductionWorkspaceV2.tsx`、`Procurement.tsx` 这类高耦合页面。
