# 爱劳达 ERP+CRM 全局治理台账

更新日期：2026-04-13

## G1 业务真值组
- 范围：订单、回款、风控、财务
- 目标：统一 `finalAmount / paidAmount / outstanding / paymentStatus / financialStatus / fulfillmentStatus` 口径
- 当前重点：
  - 订单页仍有本地推导残留
  - 风控页已开始改用 verified-only 付款历史
  - 财务 overview / ledger / cashflow 仍需继续核口径

## G2 主数据权限组
- 范围：客户、供应商、角色权限、公海/私海/内池、导入导出
- 目标：把跨国公司主档、多地址、多联系人、角色可见/可改边界收成统一口径
- 当前重点：
  - 普通 `PUT` 改池已封堵
  - 销售编辑公海客户已封堵
  - 前端只读角色误开放已开始收口
  - 供应商主数据仍需按同一主数据模型继续补齐

## G3 前端治理组
- 范围：active path、乱码、多语言、导入导出、批量入口
- 目标：收窄真实运行入口，清理 `live/current/clean` 混用，修复活跃页面乱码和脱钩
- 当前重点：
  - `BarterWorkspace.clean.tsx` 已重写为干净 UTF-8
  - `SalesOrderColumns.live.tsx` 已重写为干净 UTF-8
  - 仍需继续扫 `useSalesOrders.ts` 等活跃文件的残余乱码和旧逻辑

## G4 运行部署组
- 范围：runtime schema、备份恢复、离线运行、单机迁移、服务器部署
- 目标：确保本地离线稳定运行，并具备迁移到服务器的明确路径
- 当前重点：
  - runtime schema audit 已做，当前 `issueCount = 0`
  - 稳定启动脚本已可重复执行
  - 仍需继续审断网资源依赖、部署模板、上传文件路径与迁移策略

## G5 并发对账组
- 范围：幂等、重复提交、金额/状态对账、极限冲突
- 目标：提前打出并发/冲突/对账问题，避免上线后金额或状态漂移
- 当前重点：
  - 已做过一轮订单、回款、货抵、客户池、生产并发
  - 仍需继续做混合并发和跨模块对账

## 当前执行顺序
1. G1 业务真值组
2. G2 主数据权限组
3. G3 前端治理组
4. G4 运行部署组
5. G5 并发对账组

## 已确认的高优先级持续项
- 活跃页面残余乱码继续清理
- 订单页本地真值推导继续替换
- 供应商主数据继续向客户主数据模型靠齐
- UI 多语言与批量导入导出继续真实点测
- 运行部署与备份恢复继续做制度化校验
