# 阶段3浏览器全模块点测与语言切换收口_2026-04-23

## 本包目标

补齐上一份证据矩阵中最大的缺口：浏览器级真实操作证据。  
本包不改业务页面设计，只新增一个语言切换浏览器审计脚本，并把它接入 Phase3 浏览器闸门。

## 执行范围

| 范围 | 结果 |
| --- | --- |
| 稳定包来源确认 | PASS |
| 浏览器 human-flow 全模块 | PASS |
| 模块标题与命令入口 | PASS |
| 角色权限 UI 授权/撤销 | PASS |
| 登录后语言切换 | PASS |
| 乱码三步法复核 | PASS，发现 `Â` 为越南文合法字符，不是乱码 |

## 关键证据

| 证据 | 路径 | 结果 |
| --- | --- | --- |
| 稳定包来源 | `F:\爱牢达\output\audit\stable-package-origin-audit-v1.json` | PASS |
| 浏览器全模块人工流 | `F:\爱牢达\output\playwright\browser-human-flow-audit-report-v1.json` | PASS，6 个模块，73 步，失败 0，console error 0 |
| 浏览器全模块截图 | `F:\爱牢达\output\playwright\browser-human-flow-audit-v1` | 已生成 |
| 生产 BOM 深测 | `F:\爱牢达\output\playwright\production-browser-audit-report-v1.json` | PASS，34 步，console error 0 |
| 模块标题探针 | `F:\爱牢达\output\playwright\module-registry-title-probe-v1.json` | PASS |
| 角色权限 UI | `F:\爱牢达\output\playwright\role-system-permission-ui-browser-audit-report-v1.json` | PASS |
| 语言切换探针 | `F:\爱牢达\output\playwright\language-switch-browser-audit-report-v1.json` | PASS，30 步，失败 0，console error 0 |
| 语言切换截图 | `F:\爱牢达\output\playwright\language-switch-browser-audit-v1` | 中文、英文、越南文、切回中文 4 张截图 |

## 浏览器 human-flow 覆盖

| 模块 | 覆盖动作 | 结果 |
| --- | --- | --- |
| CRM | admin 登录、进入 CRM、新建客户、保存、列表回读 | PASS |
| 销售订单 | 依赖 CRM 建档、进入订单、创建订单、保存、回款登记、付款承诺回读 | PASS |
| 发货 | 依赖订单、OCR 创建、派车/出库、回单抽屉、回单提交、读回 | PASS |
| 采购 + 仓储 | 新增供应商、创建采购单、仓库角色登录、仓库/库位创建、入库、库存与库位筛选回读 | PASS |
| 回款 + 应收调整 | 财务角色登录、回款中心进入、应收调整创建、入账、冲销 | PASS |
| 生产 | 生产页进入、生产 smoke、化工 BOM 深测 | PASS |

## 语言切换覆盖

| 动作 | 预期 | 结果 |
| --- | --- | --- |
| 初始中文 | 页面包含 `经营总览`，localStorage 为 `zh` | PASS |
| 切换英文 | 页面包含 `Overview`，localStorage 为 `en` | PASS |
| 切换越南文 | 页面包含 `Tổng quan`，localStorage 为 `vi` | PASS |
| 切回中文 | 页面包含 `经营总览`，localStorage 为 `zh` | PASS |

## 乱码三步法记录

| 步骤 | 结论 |
| --- | --- |
| 翻译意图 | `TRUNG TÂM` 是越南文“中心”，其中 `Â` 是合法越南文字母 |
| 判断层级 | 不是源码乱码，也不是 UI 乱码，是审计脚本坏词规则过宽 |
| 修复动作 | 移除单个 `Ã` 和 `Â` 的坏词判断，保留 `�`、`â€` 和已知 mojibake token；坏词样本改用 Unicode escape，避免污染源码乱码闸门 |

## 代码变更

| 文件 | 变更 |
| --- | --- |
| `scripts/language-switch-browser-audit-v1.cjs` | 新增登录后语言切换浏览器审计 |
| `package.json` | 新增 `audit:ui:language-switch` |
| `scripts/phase3-readiness-audit-v1.cjs` | `--with-browser` 时增加 `ui-language-switch-browser-gate` |

## 当前剩余缺口

| 缺口 | 说明 |
| --- | --- |
| 浏览器点测仍不是两个月长跑 | 本包证明逐页关键流通过，不等于长期运行证明 |
| 测试数据保洁仍未执行 | 当前只允许 report-only 清单，不能直接删数据 |
| EXE 安装器仍未完成 | 当前仍是稳定包壳，不是安装器级 EXE |
| 全模块极限冲突 UI 还需补 | 并发 API 已较强，浏览器端高频重复点击、断网、刷新中断还需下一轮 |

## 决策

本包把“浏览器完全没有证据”的风险下降了一档。  
下一包应继续做：测试数据保洁 report-only 清单、长时运行日检、以及采购/CRM/生产 BOM 的极限输入与冲突注入。

