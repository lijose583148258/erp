# 下一批修复包（并行版）

更新时间：2026-04-13

## 包 P1：Active Path 收窄包

### 已确认

- `Samples.tsx` 已转为桥接层
- `TeamManagement.tsx` 已转为桥接层
- `app/appContent.tsx` 已切换到稳定文件名入口

### 下一步

1. 核对 `BarterWorkspace.clean.tsx` 是否能收成唯一实现
2. 核对 `crm/CRMCreateModal.live.tsx` 与 `crm/CRMCreateModal.tsx`
3. 核对 `crm/CRMCustomerDrawer.live.tsx` 与 `crm/CRMCustomerDrawer.tsx`
4. 核对 `sales-orders/SalesOrderColumns.live.tsx` 与 `sales-orders/SalesOrderColumns.tsx`

## 包 P2：编码污染收口包

### 已发现

- `pages/BarterWorkspace.clean.tsx` 存在字段默认值乱码
- `pages/sales-orders/SalesOrderColumns.live.tsx` 存在货币符号乱码
- 治理文档仍有部分历史编码污染

### 下一步

1. 先判定是否源码真坏还是显示层误码
2. 仅修 active path 文件
3. 保持现有设计风格，不动结构

## 包 P3：口径统一包

### 已确认

- 风控页已切到 `/collections/*` 真口径
- 货抵过账已联动订单回款状态

### 下一步

1. 继续核对 `CollectionCenterView`
2. 继续核对 `FinanceAnalyticsWorkspaceV2`
3. 核对 `SalesOrders` 列表是否还有前端自行推导状态

## 包 P4：CRM 权限流转包

### 下一步

1. 核对业务经理可见范围
2. 核对销售可见范围
3. 核对公海 / 私海 / 内池流转与收回权
