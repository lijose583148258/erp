# 30_发布候选包与冻结阻断记录_20260617

## 本次目标

将当前治理成果推进到可打包、可迁移测试、可审计追溯的发布候选状态。

## 已生成候选包

- 压缩包：`F:\爱牢达\output\packages\爱劳达纯净系统_20260617_095332.zip`
- 大小：222.82 MB
- SHA256：`5691D1E023AB1DFA95281C9013AE8F0003614121494BE1FBF60D4BBA32AD0909`
- 来源目录：`E:\爱劳达纯净系统`
- 运行数据库：`D:\AilaoDaRuntime\stable.db`

## 发布门禁结果

### 已通过

- `npx tsc --noEmit --pretty false`
- `npm --prefix backend run build`
- `npm run build`
- `node .\scripts\effective-source-mojibake-gate-v1.cjs`
- `node .\scripts\default-credential-release-gate-v1.cjs`
- `node .\scripts\auth-account-lifecycle-api-audit-v1.cjs`
- `node .\scripts\role-assignment-escalation-audit-v1.cjs`
- `node .\scripts\role-audit-diff-api-audit-v1.cjs`
- `node .\scripts\team-account-lifecycle-browser-audit-v1.cjs`
- `node .\scripts\role-system-permission-ui-browser-audit-v1.cjs`
- `node .\scripts\sales-orders-browser-audit-v1.cjs`
- `node .\scripts\masterdata-search-readback-api-audit-v1.cjs`
- `node .\scripts\procurement-browser-audit-v1.cjs`
- `node .\scripts\shipping-browser-audit-v1.cjs`
- `node .\scripts\concurrency-consistency-audit-v1.cjs`
- `node .\scripts\concurrency-reconcile-deep-audit-v1.cjs`
- `node .\scripts\phase3-package-e-audit-v1.cjs`

### 当前 Phase3 包审计

- 报告：`F:\爱牢达\output\audit\phase3-package-e-audit-20260617025547048-21864.json`
- 结果：12/12 通过。
- 重点覆盖：
  - E 盘纯净包启动来源。
  - 5001 运行时健康检查。
  - 默认凭据发布门禁。
  - 双端口与编码检查。
  - 并发一致性。
  - 深度重复提交/对账。
  - 库存流水对账。

## 本次修复

Phase3 初次失败点不是业务实现，而是两个并发审计脚本仍提交旧的支付方式字段：

- `scripts/concurrency-consistency-audit-v1.cjs`
- `scripts/concurrency-reconcile-deep-audit-v1.cjs`

已将支付请求中的 `method: 'bank'` 改为当前后端合法值 `method: 'bank_transfer'`。

修复后两个脚本均通过，并重新构建、重新部署、重新打包、重新执行 Phase3 包审计。

## 仍然阻断正式冻结

`node .\scripts\release-freeze-manifest-v1.cjs` 当前结果为 `blocked`。

阻断项：

1. `source workspace is not frozen: 102 git status entries`
2. `stable package was built from a dirty workspace: 101`

这表示候选包可用于迁移测试，但不能冒充“最终商业冻结包”。

正式发布前需要完成：

1. 审阅 `F:\爱牢达\output\audit\release-worktree-change-inventory-v1.md`。
2. 确认 101 个变更全部属于本轮治理成果。
3. 执行源码冻结动作，例如提交 Git commit 或建立正式 release tag。
4. 重新执行：
   - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\package-stable.ps1`
   - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\deploy-clean-runtime-package-v1.ps1`
   - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\package-clean-runtime-zip-v1.ps1`
   - `node .\scripts\phase3-package-e-audit-v1.cjs`
   - `node .\scripts\release-freeze-manifest-v1.cjs`

## 反幻觉发布原则

本节点对齐以下外部原则：

- NIST SSDF PS.2：发布物需要提供完整性校验信息，帮助使用方确认软件未被篡改。
- OWASP CI/CD Artifact Integrity Validation：发布物需要可验证来源与完整性。
- GitHub Releases：正式 release 应绑定到可定位的仓库历史点。

## 结论

当前已经有可迁移测试的发布候选包。

当前还不是最终冻结发布包，因为源码工作树未冻结。
