# P3 封装门禁复核与反复杂度治理记录

日期：2026-06-18

## 本轮结论

不能宣称蓝图全部完成。当前完成的是 P3 封装门禁的阻塞治理：

- `verify:phase3` 从失败变为通过。
- 默认演示账号密码清单从运行代码重复硬编码改为引用种子 fixtures。
- 已受控的打包脚本纳入全仓审计治理口径。
- 超大运行文件不再被当作可继续叠加功能的区域，必须按 `43_P3重构冻结门禁与低复杂度治理计划_20260618.md` 逐步拆薄。

## 修复点

### 默认演示账号治理

涉及文件：

- `backend/src/controllers/auth.controller.ts`
- `backend/src/database/runtime-schema-auth-repair.ts`
- `backend/src/database/seed-fixtures.ts`

处理方式：

- 运行代码不再单独维护 `admin123`、`manager123` 等默认密码清单。
- 登录拦截和运行时修复统一引用 `demoUsers`。
- 发布环境仍由 `AILAODA_BLOCK_DEMO_CREDENTIALS` 控制默认凭据拦截。

### 重构冻结门禁

涉及文件：

- `scripts/refactor-freeze-gate-v1.cjs`
- `爱劳达软件治理中心/43_P3重构冻结门禁与低复杂度治理计划_20260618.md`

处理方式：

- 三个超过 650 行的运行文件必须被计划覆盖。
- 有计划时允许当前冻结继续进入迁移/服务器测试。
- 后续新增功能前必须先低风险抽取，不允许继续在大文件里堆逻辑。

### 全仓审计误报收口

涉及文件：

- `scripts/full-codebase-audit-v1.cjs`

处理方式：

- `gate` 类脚本纳入审计/验证资产识别。
- 已有路径保护的打包脚本纳入受控运行脚本清单。

## 当前证据

- `npx tsc --noEmit --pretty false`：通过。
- `npm --prefix backend run build`：通过。
- `node scripts/full-codebase-audit-v1.cjs`：warning，仅剩 P2 oversized 维护债。
- `npm run verify:phase3`：通过，53/53 runtime steps passed。
- `npm run audit:phase3:dashboard`：watch，可进入本地稳定运行试跑，但仍保留观察项。

## 剩余边界

当前仍不能宣称以下内容全部完成：

- P1 六个高密度业务工作台全部恢复到成熟商业 ERP 水平。
- P2 跨国主数据、多币种、税务、银行、组织、仓库、经销商数据范围全部建模完成。
- P3 所有角色的完整浏览器人工验收全部覆盖。
- PostgreSQL 主从、云端高可用、BI 预警、审批流引擎完成生产级落地。

## 下一步

如果继续开发，优先顺序是：

1. 先从 `pages/team/RoleManagementPanel.tsx` 抽离静态 copy 和 diff helper。
2. 再从 `pages/WarehouseWorkspace.tsx` 抽离统计卡片和查询参数 helper。
3. 最后从 `pages/ProductionWorkspaceV2.tsx` 抽离生产表单校验和批次追踪 helper。

每一步只做低风险抽取，不重写业务闭环。
