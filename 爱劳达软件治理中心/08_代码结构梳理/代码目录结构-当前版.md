# 代码目录结构 — 当前版（2026-04-28 更新）

> 当前真实结构详见：`结构目录与运行链_2026-04-28.md`。
>
> 重要修正：本文件旧正文保留为 2026-04-15 历史口径，不能单独作为当前维护依据。2026-04-28 已把 E 盘纯净运行版、资产批次服务端分页、货抵页面拆分、人工流脚本拆分、壳审计进程守卫、生产批次真实搜索回读和阶段 3 `ready-for-local-soak` 证据纳入结构说明。

## 2026-04-28 当前口径摘要

| 层级 | 当前目录/入口 | 说明 |
| --- | --- | --- |
| 稳定访问入口 | `http://127.0.0.1:5001/` | 唯一稳定验收入口；不以 `3000`、`5173`、智仔页面作为爱劳达验收入口 |
| 纯净运行版 | `E:\爱劳达纯净系统` | EXE 和后端运行应只认此目录 |
| 稳定数据库 | `D:\AilaoDaRuntime\stable.db` | 当前运行事实库；`backend/prisma/*.db` 只作为影子库治理对象 |
| 前端源码 | `app/`, `components/`, `pages/`, `services/`, `utils/`, `translations/` | React/Vite 当前有效源码 |
| 后端源码 | `backend/src/`, `backend/prisma/models/*.prisma` | Express + Prisma + SQLite 当前有效源码和分域模型 |
| 构建产物 | `dist/`, `backend/dist/`, `AilaoDa_Stable_Package/` | 可重建产物，不作为源码事实 |
| 审计脚本 | `scripts/`, `scripts/lib/` | 启动、审计、人工流、备份恢复、Phase 3 治理闸门 |
| 治理台账 | `爱劳达软件治理中心/` | 当前阶段判断、结构目录、任务记录、证据链 |

当前应优先阅读：

1. `结构目录与运行链_2026-04-28.md`
2. `../10_主线任务/阶段3稳定运行治理看板-当前版.md`
3. `../10_主线任务/每日本地试跑审计-当前版.md`

---

## 2026-04-23 当前口径摘要

| 层级 | 当前目录/入口 | 说明 |
| --- | --- | --- |
| 稳定访问入口 | `http://127.0.0.1:5001/` | 唯一稳定验收入口；不以 `3000`、`5173`、智仔页面作为爱劳达验收入口 |
| 稳定数据库 | `D:\AilaoDaRuntime\stable.db` | 当前运行事实库；`backend/prisma/*.db` 只作为影子库治理对象 |
| 前端源码 | `app/`, `components/`, `pages/`, `services/`, `utils/`, `translations/` | React/Vite 当前有效源码 |
| 后端源码 | `backend/src/`, `backend/prisma/models/*.prisma` | Express + Prisma + SQLite 当前有效源码和分域模型 |
| 构建产物 | `dist/`, `backend/dist/` | 可重建产物，不作为源码事实 |
| 审计脚本 | `scripts/`, `scripts/lib/` | 启动、审计、人工流、备份恢复、Phase 3 治理闸门 |
| 治理台账 | `爱劳达软件治理中心/` | 当前阶段判断、结构目录、任务记录、证据链 |
| 运行资料 | `backups/`, `logs/`, `output/`, `uploads/` | 不随意删除；用于本地稳定两个月证据累计 |
| 历史资料 | `历史归档/`, `文档归档/`, `AilaoDa_Stable_Package*` | 仅用于比对和恢复，不参与当前运行入口 |

当前应优先阅读：

1. `结构目录与运行链_2026-04-23.md`
2. `../10_主线任务/阶段3稳定运行治理看板-当前版.md`
3. `../10_主线任务/阶段3本地两个月稳定运行与服务器迁移准入记录_2026-04-21.md`

---

## 历史正文：2026-04-15 口径

> 最后更新：2026-04-15 正名清理后

## 根目录

```
f:\爱牢达\
├── app/                      # React 应用入口
│   └── AppContext.tsx         # 全局上下文（语言/主题/用户/通知）
├── backend/                   # Express + Prisma 后端
│   ├── prisma/
│   │   └── schema.prisma      # 数据模型定义
│   ├── scripts/
│   │   ├── fix-db.ts          # 数据库修复工具
│   │   └── fix-db.js          # 同上编译版
│   └── src/
│       ├── config/runtime.ts  # 运行时路径配置
│       ├── controllers/       # 路由控制器（14个）
│       ├── database/          # DB 迁移/修复/审计工具
│       ├── middleware/        # 认证/限流等中间件
│       ├── routes/            # Express 路由定义
│       ├── services/          # 业务服务层
│       ├── utils/             # 后端工具（logger/TimberEngine 等）
│       └── server.ts          # 服务入口
├── components/                # React 公共组件
│   ├── DataTable.tsx          # 通用数据表格
│   ├── Login.tsx              # 登录页
│   ├── AIAssistant.tsx        # AI 助手
│   ├── CommandPalette.tsx     # 命令面板
│   └── ...
├── pages/                     # 页面组件（全部已正名，无代理壳）
│   ├── Dashboard.tsx          # 仪表盘
│   ├── CRM.tsx                # 客户管理
│   ├── SalesOrders.tsx        # 销售订单
│   ├── Shipping.tsx           # 出货物流
│   ├── Assets.tsx             # 资产管理
│   ├── Procurement.tsx        # 采购管理
│   ├── BarterWorkspace.tsx    # 货抵支付（正名自 .clean.tsx）
│   ├── Samples.tsx            # 样品管理（正名自 .current.tsx）
│   ├── TeamManagement.tsx     # 团队管理（正名自 .current.tsx）
│   ├── Contracts.tsx          # 合同管理
│   ├── TimberWorkspace.tsx    # 木材工作台
│   ├── RiskControl.tsx        # 风控看板
│   ├── ProductionWorkspaceV2.tsx # 生产管理
│   ├── crm/                   # CRM 子模块
│   │   ├── CRMCreateModal.tsx     # 正名自 .live.tsx
│   │   ├── CRMCustomerDrawer.tsx  # 正名自 .live.tsx
│   │   ├── useCRM.tsx             # CRM 核心 hook（25KB 实体）
│   │   ├── useCRM.ts              # 转发层（→ useCRM.tsx）
│   │   └── CRM*.tsx               # 其他 CRM 组件
│   ├── sales-orders/          # 订单子模块
│   │   ├── SalesOrderColumns.tsx  # 正名自 .live.tsx
│   │   └── ...
│   └── shipping/              # 出货子模块
├── services/                  # 前端服务层（API 调用）
├── utils/                     # 前端工具函数
├── translations/              # i18n 翻译字典
├── scripts/                   # 前端运维脚本
├── public/                    # 静态资源
├── 爱劳达软件治理中心/         # 项目治理文档中心
├── 历史归档/                   # 归档区（不参与构建）
│   ├── 01_旧版本/
│   ├── 02_旧目录结构/          # temp_compare_cleanmigration + 旧 src/
│   └── 03_运行时残留/          # runtime-db + scratchdb + tempdb
├── 文档归档/                   # 文档归档
└── 测试/                      # 测试相关
```

## 关键文件

| 文件 | 说明 |
|------|------|
| `App.tsx` | 应用根组件，所有路由定义 |
| `index.tsx` | React 入口 |
| `types.ts` | 全局类型定义（10KB） |
| `constants.tsx` | 全局常量 |
| `vite.config.ts` | Vite 配置（唯一，无 .fixed 副本） |
| `tsconfig.json` | TypeScript 配置 |
| `.gitignore` | 版本控制排除规则 |
| `Dockerfile` + `docker-compose.yml` | 容器化部署 |
| `正式启动.bat` / `停止服务.bat` | Windows 启停脚本 |

## 数据库

| 路径 | 说明 |
|------|------|
| `D:\AilaoDaRuntime\stable.db` | 生产 SQLite 数据库（唯一活跃） |
| `backups/` | 自动备份目录（24h 周期，30 天清理） |
| `backend/prisma/schema.prisma` | Prisma 模型定义 |

## 零代理壳声明

截至 2026-04-15，`pages/` 下所有 `.tsx` 文件均为**直接实体**（非 re-export 壳）。唯一保留的转发层是 `pages/crm/useCRM.ts` → `useCRM.tsx`，因为 `CRM.tsx` 通过目录导入解析到 `.ts`。
