# 51_P3 CRM URL 状态低复杂度拆薄治理

日期：2026-06-19

## 本次目标

继续降低 P2 体量告警，但不触碰 CRM 客户池、地址、联系人、导入导出和保存链路。

## 调整内容

- 将 `useCRM.tsx` 内的 URL 初始状态解析抽到 `crmUrlState.ts`。
- `useCRM.tsx` 从 600 行降到 577 行。
- 全代码 P2 告警从 3 降到 2，剩余两项均为审计脚本，冻结门禁建议无真实 bug 时不拆。

## 风险控制

本次只移动纯函数和类型：

- 不改客户创建。
- 不改客户池流转。
- 不改联系人和地址编辑。
- 不改导入导出。
- 不改 AI 风险洞察。

## 验证结果

- `npx tsc --noEmit --pretty false`：通过
- `node scripts/full-codebase-audit-v1.cjs`：P2 从 3 降为 2
- `npm run audit:refactor:freeze`：通过
- `node scripts/effective-source-mojibake-gate-v1.cjs`：通过
- `npm run build`：通过
- `npm --prefix backend run build`：通过
- `npm run verify:phase3`：通过

## 后续注意

剩余 P2 为审计脚本：

- `scripts/procurement-browser-audit-v1.cjs`
- `scripts/lib/browser-human-flow-modules.cjs`

这两类文件是验证资产，不是用户运行时业务模块。除非后续审计脚本本身出现真实维护问题，否则不为数字继续强拆。
