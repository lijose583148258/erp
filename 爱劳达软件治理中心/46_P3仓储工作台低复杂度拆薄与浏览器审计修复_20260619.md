# P3 仓储工作台低复杂度拆薄与浏览器审计修复

日期：2026-06-19

## 本轮目标

继续按“最终企业版可封装、代码不复杂、维护成本低”的方向治理。  
本轮不新增业务功能，只处理仓储工作台的维护性风险和浏览器审计前置条件。

## 已执行

1. 拆薄 `pages/WarehouseWorkspace.tsx`
   - 新增 `WarehouseWorkspaceHeader.tsx` 承接仓储页头部展示。
   - 新增 `WarehouseWorkspaceSummaryCards.tsx` 承接仓储统计卡片。
   - 新增 `warehouseWorkspaceHelpers.ts` 承接仓库汇总、库位扁平化、库位筛选。
   - 主文件从 678 行降至约 645 行，低于重构冻结硬限制 650。

2. 修正仓储浏览器审计前置条件
   - `warehouse-transfer-browser-audit-v1.cjs` 不再直接使用默认 `warehouse/warehouse123` 进入页面。
   - 改为创建/复用 `audit_warehouse` 专用审计账号，并完成首次改密后再执行浏览器链路。
   - 保留产品侧“默认账号首次登录必须改密”的安全逻辑。

## 验证证据

- `npx tsc --noEmit --pretty false`：通过。
- `node scripts/effective-source-mojibake-gate-v1.cjs`：通过。
- `npm run audit:warehouse:transfer`：通过。
- `npm run audit:warehouse:transfer:browser`：通过，13 步。
- `npm run audit:auth:account-lifecycle`：通过。
- `npm run audit:refactor:freeze`：通过，0 blocker。
- `npm run verify:phase3`：通过。
- `npm run build`：通过。
- `npm --prefix backend run build`：通过。

## 仍未完成

- `full-codebase-audit-v1` 仍为 P2 warning，说明全仓还有偏大文件需要继续拆薄。
- 本轮只治理仓储工作台展示层和审计脚本，不宣称 P3 蓝图全部完成。
- 后续应继续按低风险顺序拆薄生产、采购、CRM、货抵、财务等偏大边界。
