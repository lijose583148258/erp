# P3 角色授权页低复杂度拆薄记录

日期：2026-06-18

## 本轮目标

按照 `43_P3重构冻结门禁与低复杂度治理计划_20260618.md`，先处理最安全的角色授权页面，降低后续账号、角色、授权维护成本。

## 改动范围

涉及文件：

- `pages/team/RoleManagementPanel.tsx`
- `pages/team/roleManagementHelpers.ts`
- `pages/team/roleManagementConfig.ts`

## 处理方式

- 抽离 `RoleDraft`、`ChangeReview` 类型。
- 抽离角色草稿转换、集合对比、权限差异、风险权限识别等纯 helper。
- 抽离数据范围选项与权限分组标签配置。
- 保留原页面的状态管理、保存动作、接口调用、授权预览流程。

## 反复杂度约束

- 没有新增状态管理框架。
- 没有改动后端接口。
- 没有改动角色保存流程。
- 没有重写 UI 结构。
- 没有把权限逻辑拆成多层抽象。

## 当前证据

- `RoleManagementPanel.tsx` 从 662 行降到约 590 行。
- `npx tsc --noEmit --pretty false`：通过。
- `node scripts/effective-source-mojibake-gate-v1.cjs`：通过。
- `npm run audit:permissions:system-ui`：通过。
- `npm run audit:auth:account-lifecycle`：通过。
- `node scripts/full-codebase-audit-v1.cjs`：P2 oversized 从 7 降到 6。
- `npm run verify:phase3`：通过，53/53 runtime steps passed。

## 仍需继续

本轮只处理角色授权页。剩余维护债仍包括：

- `pages/ProductionWorkspaceV2.tsx`
- `pages/WarehouseWorkspace.tsx`
- `pages/Procurement.tsx`
- `pages/crm/useCRM.tsx`
- 其他超过全仓审计阈值的运行文件

后续仍按“只抽离纯展示、纯配置、纯 helper，不重写业务闭环”的方式推进。
