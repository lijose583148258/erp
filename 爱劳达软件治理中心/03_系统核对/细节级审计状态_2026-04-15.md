# 爱劳达 ERP+CRM 细节级审计状态

日期：2026-04-15

## 结论

本轮不是“全部审计完”，但已经从“发现问题”推进到“边审边收口”。

当前可以确认：

- `tsc --noEmit --pretty false` 通过
- `npm run build` 通过
- 前后端两条高优先契约残留已经收口
- 客户池审计和收款中心的用户可见技术口径又清掉了一批

当前仍不能确认：

- 浏览器逐页人工点测已全部完成
- 历史数据库中的客户池审计缺口到底是旧数据缺口，还是仍有写入漏审计
- 所有 `.current/.live/.clean` 残留都已经可以删除

## 本轮已修复

### 1. 资产单客户余额接口已对齐

已修：

- 前端 `services/asset.service.ts` 的 `/assets/balance/:customerId`
- 后端 `backend/src/routes/asset.routes.ts`
- 后端 `backend/src/controllers/asset.controller.ts`

当前结果：

- `/assets/balance`
- `/assets/balance/:id`

两条路径都存在，单客户资产余额不再天然 404。

### 2. 客户导出下载鉴权已闭环

已修：

- 前端 `services/customer.service.ts`
- 后端 `backend/src/middleware/auth.ts`

当前结果：

- 前端导出仍走 `GET /customers/export?token=...`
- 后端 `authenticate` 已支持 GET 下载场景下的 query token

判断：

这条链路从“看起来能做但一点击会坏”提升到“代码链闭环”。

### 3. 客户池审计页不再直接暴露关键技术码

已修：

- `pages/crm/CRMCustomerPoolAudit.tsx`
- `backend/src/controllers/customer.controller.ts`
- `types.ts`
- `services/customer.service.ts`
- `translations/zh.ts`
- `translations/en.ts`
- `translations/vi.ts`

当前结果：

- `POOL_ASSIGN / POOL_RELEASE / POOL_RECLAIM / POOL_TRANSFER` 已映射为业务动作名
- 当前池负责人支持显示 `salespersonName`
- 审计记录中的 `salespersonName` 已由后端补回
- 角色显示已从技术枚举改为业务角色名

### 4. CRM 顶部筛选与工作台口径已多语言化

已修：

- `pages/crm/CRMViewSwitcher.tsx`
- `translations/zh.ts`
- `translations/en.ts`
- `translations/vi.ts`

当前结果：

- `ALL / DIRECT / CHANNEL / MIXED / Workbench`

不再直接暴露给用户，改为三语业务口径。

### 5. 回款中心风险等级不再裸露英文枚举

已修：

- `pages/collections/CollectionCenterView.tsx`

当前结果：

- `low / medium / high / critical`

已映射成：

- `低风险 / 中风险 / 高风险 / 关键风险`

### 6. 调账详情页快照标题已本地化

已修：

- `translations/zh.ts`
- `translations/vi.ts`

当前结果：

- 中文：`前态 / 后态`
- 越文：`Trước / Sau`

## 当前已确认的残留

### P1：需要继续验证的真实链路

- 浏览器实际点击验证 `单客户资产余额`
- 浏览器实际点击验证 `客户导出`
- 客户池变更新写入时，是否每次都能在 audit 中看到 `POOL_*`

### P2：历史数据与审计留痕问题

- 当前数据库中已有客户存在 `poolState=private`，但历史审计只看到 `CREATE`
- 这更像“旧数据未补历史”，但还不能完全排除“部分写入路径漏审计”

### P2：残留壳文件边界还没最终裁掉

当前扫描到的后缀文件：

- `pages/BarterWorkspace.clean.tsx`
- `pages/BarterWorkspace.current.tsx`
- `pages/Samples.current.tsx`
- `pages/TeamManagement.current.tsx`
- `pages/crm/CRMCreateModal.live.tsx`
- `pages/crm/CRMCustomerDrawer.live.tsx`
- `pages/crm/useCRM.current.ts`
- `pages/crm/useCRM.live.tsx`
- `pages/sales-orders/SalesOrderColumns.live.tsx`

其中已知：

- `pages/crm/useCRM.current.ts` 是无引用薄壳，可进入删除候选
- `pages/crm/CRMCreateModal.live.tsx`
- `pages/crm/CRMCustomerDrawer.live.tsx`

仍在真实链路中，不能直接删

## 下一步顺序

1. 做浏览器级人工点测，先验证 `资产余额 / 客户导出`
2. 继续核对客户池 audit，区分“历史缺口”与“当前链路漏写”
3. 输出 `.current/.live/.clean` 三档清单：必留 / 可删 / 高风险先不动
4. 再处理 `AdjustmentFilters`、`AdjustmentDetail` 这类仍偏开发态的小口径

## 反思

这轮最大的进步，不是“修了更多页面”，而是把真正会伤使用体验的技术码、接口脱钩、下载鉴权、枚举泄露这些问题又往前推进了一步。

离“最完美”还有距离，但当前方向是对的，而且每一包都经过了类型和构建验证，没有继续把系统修坏。
