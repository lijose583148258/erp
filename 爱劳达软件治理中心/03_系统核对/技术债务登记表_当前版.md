# 技术债务登记表（当前版）

> 更新时间：2026-04-11  
> 说明：记录当前已知的技术欠账，按优先级排序，供后续迭代规划使用。

---

## 一级债务（影响安全/稳定性，需优先偿还）

| # | 位置 | 问题描述 | 影响 | 建议解法 |
|---|---|---|---|---|
| T-01 | 全部后端控制器 | 高风险接口无角色校验（仅前端隐藏入口） | 已登录用户可绕过前端直接调用任意接口 | 在 `auth.routes` 或 middleware 层加 `requireRole()` |
| T-02 | `rma.controller.ts:82` | `customerId` 写入 Prisma 未做 `Number()` 转换 | 新建售后报 500 | 加 `Number(customerId)` |
| T-03 | `sample.controller.ts:81` | `customerId` 写入 Prisma 未做 `Number()` 转换 | 新建样品报 500 | 加 `Number(customerId)` |
| T-04 | `backend/.env` | `JWT_SECRET` 如未设置则使用 hardcoded 默认值 | JWT 可被任意系统伪造 | 强制生产环境必须设置，启动时校验 |
| T-05 | 数据库 | `Customer.overdueAmount` 手动维护而非自动计算 | 逾期数据与回款实际不符 | 在 `CollectionStateService.verifyPaymentRecord` 后联动更新 |

---

## 二级债务（影响可维护性，计划偿还）

| # | 位置 | 问题描述 | 影响 | 建议解法 |
|---|---|---|---|---|
| T-06 | `contract.controller.ts:213` | `analyzeContract` 返回固定模拟数据 | 用户以为合同 OCR 真实有效 | 对接 Gemini Vision API 或标注为"开发中" |
| T-07 | `backend/src` | 多处用 `any` 类型（尤其是 `where: any`） | 类型安全漏洞，重构困难 | 逐步替换为精确 Prisma 类型 |
| T-08 | 前端路由 | 使用 `activeTab` state 管理导航，刷新跳回首页 | URL 无法分享，无法书签 | 迁移到 `react-router-dom` |
| T-09 | `docker-compose.yml` | `backend` 服务引用的 Dockerfile 不存在（`backend/Dockerfile`） | Docker 部署时后端无法单独构建 | 为 `backend/` 目录新建独立 Dockerfile |
| T-10 | `docker-compose.yml` | CORS_ORIGIN 写死为 localhost | 部署到真实服务器必须手改 | 改为读环境变量 `${CORS_ORIGIN}` |
| T-11 | `CurrencyService` | BOC官网爬虫依赖 HTML table 标签正则解析 | 一旦中行更新官网样式将导致抓取失败并回退 | 建议引入专业的企业级付费汇率 API 或监控系统警报 |

---

## 三级债务（锦上添花，有空可做）

| # | 位置 | 问题描述 | 建议 |
|---|---|---|---|
| T-11 | 根目录 | 约 20 个 `.tmp-*.cjs` / `tmp_*.ps1` 调试文件残留 | 批量删除 |
| T-12 | 开发模式 | `VITE_USE_MOCK=true` 为默认值，不写 `.env` 就走 Mock | 改默认为 `false`，加文档说明 |
| T-13 | 前端 | `ClickSpark` 粒子过多过大（8个, 8-18px） | 降至 2-3 个, 4px |
| T-14 | 前端 | `Login.tsx` 角色按钮与快捷账号按钮冗余 | 合并为一键登录 |
| T-15 | 后端 | 无集中式输入验证层（`express-validator` 或 `zod`） | 补充请求 schema 校验 |
| T-16 | 监控 | 无生产级健康监控（Prometheus / 飞书告警等） | 接入简单的 uptime 监控 |
| T-17 | 数据 | `dunningLevel` 无自动升级逻辑 | 接入定时任务按账期自动升级 |

---

## 偿还进度追踪

- [x] T-02 / T-03（同类型 Bug #001）：出货物流 customerId 已修复（2026-04-11）
- [ ] T-02：售后申请 customerId 未修复
- [ ] T-03：样品申请 customerId 未修复
- [ ] 其余所有项：待规划
