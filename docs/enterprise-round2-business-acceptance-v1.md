# 第二轮企业业务验收 v1

## 范围与证据边界

- 基线：`ba92cbc`，第一轮企业云 #199。该结果不是本轮 12 条链验收结论。
- 固定清单：`scripts/config/enterprise-round2-v1.json`。20 个岗位名额，12 条业务链；岗位权限矩阵尚未被通用角色替代证明。
- 每个子检查都有唯一 ID。`not_run`、`blocked`、`unsupported`、超时、缺少证据均不得判通过。独立检查在其他检查失败后继续执行。
- 本轮库存压力探针使用独立的 20 个仓库账号；这是资源争抢队列，**不是**让老板、财务、销售获得仓库写权限，也不等于岗位协作模型完成。
- SQLite 双进程实测只证明该环境的结果；不能替代 PostgreSQL 双实例、浏览器业务回读或数据库重启验收。
- fixture 可创建测试账号；真实业务动作必须经过 API。数据库连接仅用于独立回读，不通过改库“修好”场景。

## 已确认业务口径：可以预售，不能虚构现货

用户确认允许超卖/预售。区分订购数量、承诺数量、现货数量、已占用数量、待补数量、已发数量、已取消数量。

1. 缺货订单可以进入明确的预售/待补履约状态；不能仅因没有现货就全局拒单。
2. 未交付数量必须能关联补货采购、生产工单，或经确认的替代品、分批交付、取消退款方案，记录责任人、承诺日期和进度。
3. 补单只能消解原订单未履约义务，不能重复增加销售金额、应收或已发数量；变更必须保留前后记录。
4. 实物出库量不能超过当前可用且已放行的库存。预售不授权负实物库存，不代表已经出库或已经签收。
5. 预收款、已核销款、应收余额与未履约数量分别表达；取消与退款按真实业务事件处理。
6. 验收例：现货 100 kg、订购 150 kg，允许保留 50 kg 待补并关联补货；首次只能实际发出 100 kg，补货后续发 50 kg。取消或替代必须有明确记录。

上述条目是验收要求，不是声称系统已经实现全部状态。新增审批权、超卖额度和会计确认政策需按后续产品规则落地，测试框架不替业务系统自动授权。

## 等价换物必须独立核对货与价值

复用现有协议、结算、过账、抵扣和冲正模块，禁止另起一个简化版绕开治理。

- 双方货物各自绑定物料、单位、批次、质量状态、交付数量与估值依据；数量不必相等。
- 交换估值与库存账面成本不是同一个数；等价不意味着零成本或零毛利。
- 明确币种、汇率、计价/含税口径、双方价值、可抵扣额度及差额补款；不把非现金抵扣伪造成现金收款。
- 出入库与债权债务抵扣逐笔关联；部分履行不能整单结清。单边失败不得留下无对应事实的库存或核销。
- 重复审批、过账、抵扣、冲正只产生一次业务影响；已消耗的换入货物不能通过撤销制造负库存，应进入可追踪的退换/调整流程。
- 等值、差额、部分履行、隔离批次、预售待补、重复提交、撤销失败都必须测试。

## 首批实测

1. `stock-20-contention`：20 独立身份同步抢同一批 100 kg，每次调出 10 kg；最多且预期恰好 10 次真实成功。核对余额、凭证、流水、重放。
2. `transfer-shipping-contention`：同一 100 kg 批次，调拨 60 kg 与销售发货 60 kg 跨实例同步提交，只能一个成功，失败方不得留下流水。
3. `shipping-cost-conservation`：独立发货夹具核对库位余额、产品批次余额及成本台账，不把“库存扣了”当成成本正确。
4. `payment-duplicate-verification`：两个独立财务身份跨实例并发核销同一回款，回款记录和已收金额只能各形成一次应有影响。审计记录与外部事件单列 `payment-event-audit-once`，不能由金额正确推导通过。
5. `barter-dual-stock-posting-replay`：两个财务跨实例同时过账等值换物，核对双向库位/批次/成本、抵扣和重放。双方货物数量不同；我方估值不覆盖账面成本。
6. `barter-offset-cash-difference`：200 元对价换回 150 元货物，只抵扣 150 元，50 元保持应收，不能伪装成现金已到账；反向差额、部分履行另列未完成项。
7. `barter-reversal-conservation`：并发冲回后恢复原数量、原成本及应收，保留抵扣和冲回历史；允许重复请求幂等返回或明确冲突，但副作用只能一次。
8. `barter-consumed-receipt-reversal-blocked`：换入 20 kg 后真实发出 10 kg，再请求整笔冲回必须拒绝；对比事务前后的库存、批次、成本、抵扣、回款状态和冲回日志，不能出现半笔冲回。

### 本次修复的适用边界

- 修复发货、货抵时库位库存已变而批次/成本没有变化的问题；批次与成本写入使用原业务事务。
- 货抵换入成本取已审批换入明细估值；换出使用原库存账面成本，不使用售价。
- 冲回读取原库存凭证关联的成本记录并反向记账，末行吸收分摊尾差。历史货抵缺少成本证据时返回 `BARTER_COST_REVERSAL_REQUIRES_RECONCILIATION`，必须先做有凭据的历史对账，不能自动虚构成本。
- 本次不迁移历史脏账、不改变税务/汇率规则、不把预售订单错误地改成库存不足拒单。
- 只有数量、没有期初估价的历史库存不得被默认当作零成本销售；发货/货抵前库存数量必须与成本台账数量一致，否则返回 `STOCK_COST_RECONCILIATION_REQUIRED`，整笔交易回滚，先补齐估价或历史对账。

运行 `npm run test:contract:enterprise-round2` 检验裁决框架；运行 `npm run audit:enterprise:round2:local` 构建新鲜后端并创建一次性双进程 SQLite 沙箱。完整裁决只要还有未测项就返回非零；不以局部探针全绿冒充整轮通过。

企业云 `Enterprise Cloud Sandbox` 的手动触发/复用工作流可设置 `round2: true`。第二轮在独立步骤执行，失败不截断后续备份、晋升等检查；最终汇总强制包含该步骤。默认基线审计与完整第二轮裁决不混为一谈。

## 2026-09-25 本地复现记录

所有运行使用全新 SQLite 数据库、两个新后端进程、随机测试账号；没有修改稳定环境的数据。

| 运行目录（仓库内） | 结果 | 证据 |
| --- | --- | --- |
| `output/round2/1790301123793-b433af80` | 3 通过、1 失败 | 发货后库位 40 kg，但批次仍 100 kg、成本仍 1000；应为 40 kg / 400 |
| `output/round2/1790301353274-9d30c0cd` | 5 通过、2 失败 | 发货已修复；货抵批次/成本缺失被独立核对发现 |
| `output/round2/1790301763500-dae77deb` | 8 通过、29 未完成 | 出库、货抵过账/冲回、已消耗货物拒绝整笔冲回均有 API 与数据库回读；总体仍为 incomplete |
| `output/round2/1790304943471-04e29746` | 8 通过、29 未完成 | 最终复测增加无估价库存拒绝出库/全事务回滚和批次耗尽成本清零；没有隐藏未完成检查 |

报告文件均为对应目录的 `enterprise-round2-v1.json`。最后一轮同时记录源码指纹、脏工作区标记、构建时间和两个实例地址；`commit` 不能单独代表未提交源码。

最终完整后端单元测试：72 套 / 268 项通过，日志 `output/round2/unit-1790304957392/jest.log`；第二轮裁决框架 7 项通过，云拓扑契约、前端 TypeScript、有效源码编码检查通过。

### 尚未交付的范围

- 预售有库存不足也能接单的业务口径已确认；结构化待补数量、负责人、补货/替代/取消关联和完整关闭流程尚未实现并验收，不能据此宣传已上线。
- 货抵负差额、部分履行、全角色浏览器证据仍缺；税务/汇率不由本次测试推断正确。
- 20 人岗位协作不是 20 个仓库压力账号；BOM 冻结、双工单同批领料、单位换算、损耗、QC、数据库重启和完整账实复核仍须逐项推进。
- SQLite 本地结果不替代 PostgreSQL 企业云结果，更不替代正式上线验收。

## 2026-09-25 企业云复现与第二次修复

- 企业云运行 `36089142015`（提交 `9bf6196`）：固定源码 MinIO 镜像构建、依赖健康检查通过；第二轮 PostgreSQL 双实例探针 **8 通过 / 0 失败 / 29 未执行**，12 条完整链仍为 incomplete。
- 21 项业务汇总为 16 通过 / 5 失败。实际发货夹具未提供期初成本，服务返回 `STOCK_COST_RECONCILIATION_REQUIRED` 并保持待发货；依赖切换测试复用该夹具，继而导致切换与准入裁决失败。不能把这些连带失败写成 5 个独立业务缺陷。
- 另确认人工流程内部失败截断：Actions 默认 `bash -e`，`set -uo pipefail` 不会关闭已有的 `-e`。注入第 7 项失败可复现只运行 7/9 项，回款与小数台账检查缺失。修复为在 `if` 条件中执行子命令并汇总退出码。
- 新回归测试按真实 Actions shell 参数启动 Bash，分别注入第 1、7、9 项失败，均要求 9 项全部执行且最后非零。云端 source job 同样运行该契约，避免静态正则检查冒充可执行证据。
- 发货测试夹具通过原库存 API 显式记入 24 kg × 10 的测试成本，不补写历史真实数据、不放松出库一致性保护。浏览器实际发货 12 kg 后，进一步回读库位、批次、成本数量与金额，并要求恰好一条 -12 kg / -120 成本出库记录。
- 证据：`output/review/round2-cloud-36089142015/`、`output/review/human-workflow-errexit-before.json`。新增修复尚须以重新提交后的云端结果验收。

### d1ab4ad 的独立浏览器复测

- 每条链单独创建新的 SQLite 数据库、随机端口、上传/日志目录，重新构建前后端；未使用已有业务数据库。
- 发货：`output/round2/1790320231031-c36cd253/output/playwright/shipping-audit-report-v1.json`，实际浏览器 OCR 建单、发货、签收凭证上传与签收事件回读通过。库存/批次/成本数量均 12 kg，成本余额 120，出库成本 -120 且凭证恰好一张；人工查看 `shipping-final.png` 确认签收 12/12、剩余 0。
- 回款：`output/round2/1790320339229-23b07230/output/playwright/collection-center-human-flow-browser-audit-report-v1.json`，被旧工作流截断的回款工作台链已单独运行通过。该结果不是第二轮重复提交、数据库重启与外部事件唯一性的证明。
- 裁决框架现为 10 项通过，其中包含真实 Bash 故障注入与发货成本反例测试。

### PO 并发修改的发现时边界（b9ac2f5）

核对 `backend/src/routes/procurement.routes.ts`、`services/procurement.service.ts` 和采购工作台后，现有入口为新建、状态流转、收货及 B2B 关联；不存在对已建 PO 数量/单价/交期的版本化修改入口。因此不能拿状态流转的比较更新保护，替代 `po-stale-edit-conflict` 的改价/改量验收。

下一实现必须同时覆盖：服务端原子版本校验、浏览器携带原版本、冲突后展示差异而非自动覆盖、已审批变更重新审批、已收货/已入账明细禁止原地改写，以及同事务前后值审计。预售的关联补单还需独立保存待补数量与履约责任，单有 PO 关联销售单字段不等于待补闭环。

### 企业云最终回读：36105906037

- 运行链接：https://github.com/lijose583148258/erp/actions/runs/36105906037 ，实际执行提交 `d1ab4adc9c83b0e809af78a283ac93d56deebb65`。
- 21 项汇总 **20 通过 / 1 失败**；唯一失败是显式启用的第二轮完整证据门禁。20 项原有基线全部通过，不能表述成完整企业验收通过。
- PostgreSQL 双实例第二轮 **8 通过 / 0 失败 / 29 未执行 / 0 条完整链通过**。保留 incomplete 和非零退出，不为了让 CI 变绿删除待测项。
- 云端发货成本回读：库位 12 kg、批次 12 kg、成本数量 12 kg、成本余额 120、唯一出库凭证成本 -120。回款人机流程、搜索/对象存储切换、数据库切换窗口裁决均通过。
- 工作流日志确认 `collection-human-flow` 和最后的 `decimal-shadow` 已执行，不再被前序失败的 shell 语义截断。
- 下载证据位于 `output/review/round2-cloud-36105906037/`；日志为 `output/review/round2-job-107978290675.log`。本段记录不改变实际受测提交，也不把后续文档提交视为受测业务代码。

## PO 版本化修改与重新审批（下一轮增量）

### 实现边界

- 新增采购条款修订入口，允许修改数量、单价、明确复核的税额和交期。供应商、物料、单位、币种、汇率、附加费用和销售关联不在此入口改写。税额需要操作人显式确认，不由测试框架猜测税务规则。
- 提交必须带 `expectedRevision` 与读取时的 `expectedUpdatedAt`；条件更新同时匹配版本、时间和状态。一个成功者生成一个新版本，失败者返回 409，不自动将旧请求重放到新版本。
- 仅待审批或已审批且从未收货的订单可改；已发运、已收货、已取消或已有任何收货记录（含拒收）禁止原地修改。采购版本、重新计算的到岸成本与前后值审计同一事务提交，审计失败则整笔失败。
- 修改后状态回到 `pending`。修订号大于 0 时，审批/发运等状态变更必须明确提交所审版本；旧客户端缺省版本只能用于原始版本 0。B2B 自动状态同步不能绕过修订后的人工重新审批。
- 前端采购行“版本”入口展示历史。冲突时保留输入，并排显示最新值与本地值；用户核对后才可更新基准，不能自动覆盖。保存后仍待审批，审批事件标明具体修订号；同状态重试不重复生成审批审计。
- SQLite 使用增量 schema repair；PostgreSQL 使用 `202609250001_purchase-order-revision` 加列迁移，历史单据从版本 0 开始，不改历史业务金额。回滚应用时应禁用新入口，保留新列与审计，不删除修订历史。

### 已执行与未执行

- `po-stale-edit-conflict`：两个独立采购身份通过两个服务实例同时改同一已审批 PO，要求一个 200、一个 409；校验只保存获胜者的数量、单价、成本及一条前后值审计。追加旧提交重放、旧版/缺省版本审批、未审批收货、销售越权、审批重放及收货后改价拒绝检查。
- `po-reapproval-browser`：两个独立浏览器登录采购账号并读取同一版本；甲保存后乙旧页面必须冲突并保留输入；乙核对差异、保存版本 2、通过界面重新审批，浏览器/API/数据库共同回读数量 80、单价 9、成本 720、两条修订和一条重新审批记录。
- 两采购身份暂复用现有 manager 权限体系；这不证明 20 岗位的最小权限隔离。采购并发链通过也不代表部分收货/QC/应付整链或预售补单闭环通过。
- 开启本地浏览器检查：PowerShell 设置 `$env:ROUND2_BROWSER='true'` 后运行 `npm run audit:enterprise:round2:local`；前后端重新构建到本次隔离目录。云端 round2 步骤同样显式开启浏览器检查。
- 本地 `output/round2/1790322241342-d5141672/enterprise-round2-v1.json`：10 通过、0 失败、27 未执行；R2-03 的两个固定检查通过，其他链仍未完成。截图 `conflict.png`、`reapproved.png` 已人工查看。
- 更早的 `output/round2/1790322165349-71f85d02` 记录浏览器探针参数错误（启动次数设成 0），没有伪装为环境通过；改为实际启动后重新跑完整隔离链。
- 最终源码复测 `output/round2/1790322438451-54f2a38f/enterprise-round2-v1.json`：10 通过、0 失败、27 未执行；完整后端 73 套 / 281 项通过（`output/round2/unit-1790322452539/jest.log`），裁决契约 11 项、迁移/云拓扑/源码编码检查通过。以上是本地证据，PostgreSQL 企业云必须另行回读。
- SQLite 旧结构增量演练：对上述隔离测试库使用 SQLite backup API 生成一次性副本，仅在副本移除新 `revision` 列来模拟旧结构，然后调用真实编译后的 `repairCoreSchema` 两次。首次加列、再次识别已存在；2 张采购单除新增版本 0 外的全部字段逐值不变，`integrity_check=ok`、外键检查无异常。证据 `output/review/po-sqlite-upgrade-1790322869409/report.json`；这是模拟旧结构的 SQLite 证明，不等于真实历史库或 PostgreSQL 升级验收。

### 企业云业务结果与视觉复核：36109603244

- 运行 https://github.com/lijose583148258/erp/actions/runs/36109603244 的业务代码为 `199f0c97bb4e9f52089cbdbb69fc72391b841b9d`。21 项汇总 20 通过 / 1 失败，唯一失败是未完成的第二轮；PostgreSQL 双实例报告 10 通过 / 0 失败 / 27 未执行，R2-03 的两个程序断言通过。
- 同版本并发修改返回一个 200 和一个 409；浏览器保存版本 2 并重新审批后，两个 API 实例与数据库均为数量 80、单价 9、到岸成本 720，审计包含两次修订和一次版本 2 审批。
- **视觉复核没有通过**：下载的 `conflict.png` 和 `reapproved.png` 中中文显示为缺字方框，DOM 文本断言没有发现。不能据程序断言将该云端 UI 宣称完整验收通过；保留原报告，不回写或美化既有证据。
- 问题定位为浏览器渲染/字体覆盖层：相同源码在 Windows 截图中文正常、云端 DOM 中文正确但栅格图缺字。新增云端 `fonts-noto-cjk` 安装及 fontconfig 检查，不修改业务文案；最终根因需新云运行的字体匹配记录、字形像素和实际截图共同确认。
- 新增 Canvas 字形像素门禁：使用当前页面实际字体分别渲染 10 个不同中文字符，与缺字参考比较；空白、同形方框或缺失参考均拒绝通过。结果附于浏览器报告，仍需人工查看截图，不将该抽样当作全应用字体覆盖证明。
- 字体准备步骤 `continue-on-error`，不得截断后续业务审计；新增 `browser_fonts` 独立汇总项，失败/跳过/取消即最终失败。因此后续基线总数为原有 20 项 + 字体准备 1 项，开启第二轮共 22 项，第二轮 37 项义务不变。
- 新门禁本地复测 `output/round2/1790335146369-378493cf/enterprise-round2-v1.json`：10 通过 / 0 失败 / 27 未执行，两个浏览器均记录 10 个不同且非缺字的中文像素签名；已查看冲突截图中文正常。裁决/字体反例契约 13 项通过。新字体补丁尚待推送后的云端复测，不借用 `199f0c9` 的结果。

### 字体修复云验收与采购布局后续修复（36128887378）

- 实际受测提交 `36e8e6bd1269f053e77e34e05cebdd783adcb977`，运行 https://github.com/lijose583148258/erp/actions/runs/36128887378 。fontconfig 命中 `NotoSansCJK-Regular.ttc / Noto Sans CJK SC`，两浏览器各有 10 个非缺字且不同的中文像素签名；已人工查看 `reapproved.png`，中文、版本 2、数量 80、单价 9 与审批历史均可读，缺字问题已在此范围确认修复。
- 汇总为 **22 项中 20 通过 / 2 失败**：`human_workflows` 与未完成的 `round2_business`。第二轮仍为 10 通过 / 0 失败 / 27 未执行。人机链的实际失败仅为 UI audit 的采购平板横屏列按钮被侧栏拦截；后续订单、采购、调拨、发货、回款和小数账检查仍执行，失败没有截断。
- UI 报告为 `output/review/round2-cloud-36128887378/ui-ux-audit/20260925112748734-37874-80tscq/report.json`：5 路由 / 40 尺寸运行 / 114 状态 / 1 错误。中文字体恢复后暴露真实布局遮挡，不能删掉该检查或使用强制点击来让它通过。
- 新增真实布局探针在改动前本地复现 1024px 供应商列按钮越过自己表格边界（`output/round2/1790337819984-7748bc6e`）。供应商和采购订单两栏由 lg 改为 xl 启用，表格/卡片加 `min-w-0`；小屏上下排列，不调整按钮 z-index 绕过面板。
- 真实列切换又发现首次访问 `readStringArrayPreference` 将不存在的值解析为 `[]`，导致界面显示首列但选列状态全空。修复缺省、空数组及非法偏好回到默认列，保留已有非空选择；前端 18 项测试通过。
- 本地 `output/round2/1790338348847-332929fb/enterprise-round2-v1.json`：10 通过 / 0 失败 / 27 未执行，1024×768、1280×720、1440×900、768×1024、390×844 两工作台共 10 个组合均完成正常指针点击、弹层边界检查、切换列及恢复，随后 PO 版本冲突与重审链通过。
- 复核该运行的布局截图时发现部分截图仍处于启动 loading 遮罩淡出阶段；实际交互通过不等于截图有效。随后补入等待 `#loading` 隐藏、表格完成加载，并在成功切换后补截图。这一最后的证据采集调整尚未重新执行，不能沿用前次截图宣称视觉通过。
- 新增布局修复尚未提交/推送；完整 5 路由 × 8 尺寸隔离复测启动时，自动审批服务再次返回工作区额度耗尽，命令没有执行。最新静态检查（前端 TypeScript、脚本语法、YAML、diff check）通过。仍须额度恢复后补跑、更新 graphify、提交并重新云验收。
- 另保留一次本地双实例 SQLite WAL 初始化 `database is locked` 日志：`output/round2/1790337731488-4301366a`，该次未达到 readiness、未进入任何审计，未计为通过；单次重试后才进入布局探针。这不是数据库重启恢复链通过的证据。

### 额度恢复后的最终布局复测（2026-09-25）

- 最新源码（含加载完成等待与列切换后截图）双实例回归：`output/round2/1790342052682-def547e4/enterprise-round2-v1.json`，10 通过 / 0 失败 / 27 未执行，R2-03 通过。10 个工作台/尺寸组合全部完成真实点击、弹层边界和列切换恢复；已查看 1024px 供应商、采购菜单及 390px 采购菜单截图，中文可读且没有启动遮罩。
- 完整 UI 专项：`output/round2/1790342127457-ff899b85/output/ui-ux-audit/20260925131548857-10764-lzyk87/report.json`，5 路由 / 40 尺寸 / 124 状态 / 124 截图，0 错误、3 警告。警告均为 CRM 三种手机尺寸缺少复杂表格任务卡替代视图，保留，不等于全部 UI 无缺陷。
- UI 专项首次夹具缺少 APP_URL 结尾斜杠，登录 Invalid URL、0 路由；证据 `1790341936227-c8ad1e9e`。修正夹具后 `1790342026358-a46fbaf9` 再次遇到双实例 SQLite WAL 启动锁冲突，未进入 UI 审计。最终 UI 专项明确只启动单实例，不能当作双实例启动/恢复通过；上面的第二轮仍运行原始双实例入口，未绕过并发检查。
- 前端 18 项、第二轮裁决契约 13 项、前端 TypeScript 与脚本语法检查通过。本次布局与默认列修复可提交，仍须重新推送后的 PostgreSQL 企业云回读；部分交付提前整单 delivered 的业务缺陷未被此修复覆盖。

### 预售/分批履约新发现：部分交付被提前标为整单已交付（待修复）

- 受测业务源码 `36e8e6bd1269f053e77e34e05cebdd783adcb977`，隔离双实例 API 证据 `output/round2/1790335675235-786adb2f/enterprise-round2-v1.json`。这是独立缺陷复现，不是完整 37 项报告，不并入字体修复云运行的通过数量。
- 实际操作：独立销售账号建单 100 kg，管理员审批；库存 API 只记入 40 kg × 10；发货 API 创建并出库 40 kg，签收事件确认 40 kg；最后从两个实例及数据库重新读取。
- 结果：数据库订单 `status=delivered`，两个 API 均 `status=delivered / fulfillmentStatus=delivered`，订单明细仍 100 kg、签收仅 40 kg，尚欠 60 kg。物理库存与成本余额均为 0，故这次定位为履约状态提前关闭，不是负库存或成本扣错；本次未执行手动 `completed` 结案，不混淆两个状态。
- 根因入口：`syncOrderShipmentState` 仅检查已有发货单是否全部 delivered，不核对订单行应交数量；`getCommercialFulfillmentStatus` 也采用同类判断，读模型和写模型同时错误。`ORDER_DETAIL_INCLUDE` 还未查询发货行数量，单修页面文案不足以关闭缺陷。
- 首次夹具遗漏私海客户负责人被 400 拦截，第二次让建单人自行审批被 403 拦截；均保留失败证据，补齐负责人和独立审批人后才复现业务缺陷。没有绕过职责分离验证。
- 下一修复范围：按订单行核对已发、实际正常签收和待交数量；仅全部有效需求履行/获批取消时才能整单结案；多物料不同单位不能直接累加；未绑定订单行、拒收、异常、撤销及并发分批发货均不能冒充完成。部分交付后剩余数量必须继续可补货、分批履行或走有审批/退款凭证的调整。
- 同时需要覆盖 `getAvailableForShipping`（当前只选 confirmed 且已部分/全部付款）与 `completeOrder`（当前按 shipped/delivered + paid 准入）的边界，防止修复写模型后待补订单仍从选择器消失或可提前手工结案；这两个入口目前只有源码核对，尚未完成专项运行验证。
- 预售允许超过现货接单的口径不变；待补数量、负责人、预计日期和补采购/生产/替代/取消的关联方案仍待实现。等价换物的部分履行、负差额和浏览器整链也仍未完成。

### 采购布局云端回读：36140162473

- 受测提交 `ae714e766cf27c2ea9496fea6eecfaffc8bebeca`，运行 https://github.com/lijose583148258/erp/actions/runs/36140162473 。下载原始汇总为 **22 项中 21 通过 / 1 失败**，唯一失败为 `round2_business`；不采用 continue-on-error 后的绿色 conclusion 代替 outcome。
- 第二轮 PostgreSQL 双实例仍为 10 通过 / 0 失败 / 27 未执行 / 1 条完整链，采购 10 个工作台/视口组合及版本重审均通过。已查看云端 1024px 采购列菜单截图，中文可读、菜单未被侧栏遮挡。
- 全 UI 5 路由 / 40 尺寸 / 116 状态 / 116 截图，0 错误、9 警告；警告是 CRM、订单、采购在各 3 种手机尺寸缺少复杂表格任务卡替代视图。不是无警告或完整 UI 验收通过。证据保存在 `output/review/round2-cloud-36140162473/`。

## 分批履约数量门禁增量（2026-09-26，本地验证完成，待企业云）

- 以订单行 ID 和规范单位核对已分配、已发出、正常签收、未分配及待交数量；不把不同物料、kg/桶相加，不以已有发货单都 delivered 判整单完成。签收拒收、单位不一致、未绑定行和缺证据保留人工核对标志，不能直接结案。
- 物流事务锁定共享订单后重读数量汇总；手动 delivered/complete 在同一事务中先条件更新、再核对完整数量，失败回滚。结案还要求已全额付款，未改变预售允许超过现货接单的规则。没有回写真实历史数据的迁移。
- 真实订单页为 `OperatingDataGrid`，不是旧 `SalesOrderColumns`：显示每行发出数量与总量、部分交付及待交数量；前端保留后端计算的履约轴，不再用旧状态覆盖它。旧接口没有数量证据时不能在前端自行推断全部交付。
- 早期专项 `1790343100167-9406ce59`、`1790343252922-331b63c5` 的 API/数据库断言通过，但截图有固定列遮挡及启动遮罩，未判视觉通过。最终增加居中滚动、等待遮罩消失和 elementFromPoint 可见命中检查。`output/round2/1790398846897-7e20ff89/enterprise-round2-v1.json` 专项通过，已查看两张截图：40/100 kg 时部分交付、待交 60 kg；补货完成后 100/100 kg、已交付。
- 该专项销售建单、独立审批、全额回款核验、40 kg 首次交付、提前结案拒绝、60 kg 补库存、两个实例各签收 30 kg 和最终结案均走 API；浏览器仅做真实页面读回，不能称浏览器续发/补单写入链已完成。库存/批次/成本数量余额 0、成本金额余额 0，两张出库成本合计 -1000。
- 独立 UI 回归 `output/round2/1790398894805-4123a629/output/ui-ux-audit/20260926050202928-22972-055tot/report.json` 为 5 路由 / 40 尺寸 / 124 状态、0 错误、3 个 CRM 手机任务卡警告。此证据早于随后新增的历史 delivered 补发守卫及微量行修复，最终包仍须重跑。
- 独立复核发现两个需要补测的边界：历史 100/40 订单已存 delivered 时，待发货查询能看到但创建接口拒绝；固定 1e-6 容差会吞掉合法微量未发需求。增补创建事务内关联订单锁与状态校验（包括只传 orderItemId），历史 delivered 仅能在明确、无争议的行剩余量内续发；微量需求使用浮点相对容差且每行必须有正签收证据，不允许零发货被判完整交付。
- 新增独立 `sales_partial_fulfillment` 云门禁，不占用或冒充原有 37 项义务；届时基线 22 项、开启第二轮总计 23 项。步骤失败不截断后续审计，在最终汇总统一判失败。
- 尚未完成：销售页面的续发按钮/quickShip 仍存在旧整单数量拼装路径；负责人/预计日期/补采购或生产计划/替代及取消退款的结构化待补闭环、历史异常单人工修复、拒收后部分替换与等价换物整链仍待实现。上述功能不计为已通过。

### 合并后主代理复核（2026-09-26）

- `output/round2/1790399220255-bcb0cf9c/enterprise-round2-v1.json`：专项通过；在真实 40/100 部分交付读回之后，**仅对本次隔离夹具**注入旧版本产生的 delivered 状态，核实两个 API 不把它解释为全部交付、手工结案返回 409、正确行的 60 kg 可以继续创建发货。补发签收完成后再结案，最终 completed；只传 orderItemId 试图再创建发货也返回 409。未批量修复或改写真实历史库。
- 实际浏览器读回截图已由主代理查看：部分交付、待交 60 kg 清晰可见；坐标命中检查通过，无启动遮罩。库存余额、批次余额、成本数量及成本金额均为 0，唯一两张出库凭证共 -100 kg / -1000。
- `output/round2/1790399260036-aa02564f/enterprise-round2-v1.json`：原始双实例第二轮 10 通过 / 0 失败 / 27 未执行，1 条链通过，按设计返回 incomplete/非零退出。没有把独立分批专项凑入 37 项数量。
- 完整后端 75 套 / 335 项通过，日志 `output/round2/unit-1790399268748/jest.log`；前端 20 项、裁决契约 14 项通过；前后端 TypeScript、源码乱码扫描及 YAML 解析通过。知识图谱已更新，输出不提交。新提交仍须重新推送后做 PostgreSQL 企业云专项验证。
- 最终合并包 UI：`output/round2/1790399339946-e8911af9/output/ui-ux-audit/20260926050925005-8496-la4ibi/report.json`，5 路由 / 40 尺寸 / 124 状态 / 124 截图，0 错误、3 个 CRM 手机任务卡警告。独立只读复核没有发现阻断本次防提前结案和数量读回范围的新回归，不扩展为完整预售产品验收。

### 分批数量门禁企业云：36224923802（保留失败）

- 受测提交 `3885b3ca22e709aa0650e4550ee78fdbcaa3cf10`，运行 https://github.com/lijose583148258/erp/actions/runs/36224923802 。原始 `business-audit-summary.json` 为 **23 项中 18 通过 / 5 失败**，不是预期中的仅完整性门禁失败。
- 新增 `sales_partial_fulfillment` 通过：40/100 kg 尚欠 60 kg，complete/delivered 提前关闭均 409；旧 delivered 夹具仍可绑定订单行补发，正常签收满 100 kg 后才 completed。库存/批次/成本数量和成本金额余额均 0。主控已查看云端部分交付截图，中文可读、待交 60 kg 可见。
- 第二轮仍为 **10 通过 / 0 失败 / 27 未执行 / 1 条完整链通过**；UI 为 5 路由 / 40 视口 / 116 状态与截图，0 错误、9 个移动任务卡警告。
- 失败项是 `human_workflows`、`degradable_failover`、`postgres_cutover_verdict`、`enterprise_pilot_verdict`、`round2_business`。后两个技术裁决没有被人工改绿，`productionReady=false`。
- 定位线索：原发货浏览器夹具只绑定 orderId，未绑定 orderItemId；新数量门禁正确拒绝把此类缺证据发货视为整单完成。搜索/存储切换中复用了同一发货审计，失败随后传递到切换和试点裁决。修复只能补齐新夹具真实行身份，不能放松业务数量门禁；同一路径的发货报告被后一次运行覆盖，初次运行结果须查原日志，不能靠最后报告猜测。
- 证据目录 `output/review/round2-cloud-36224923802/` 保留，不用本地新 UI 结果冒充该云端提交的结果。

## 订单行级续发入口增量（2026-09-26，待新提交云端验证）

- 移除旧 `#/shipping?...` 地址和跨物料/单位求和；实际订单履约行直接打开发货草稿弹窗。打开时重新读取订单，操作者明确选择订单行、数量和真实批号；同一行只能使用服务端未分配量，已在途数量不能重复分配。
- 草稿只生成 pending 发货单，不扣库存、不替代实际出库和签收。既有 shipping.write 权限保持不变：普通销售可见待交数量但不能创建，管理员/经理可操作；未扩展仓库角色订单权限，因此不称完整销售→仓库交接完成。
- 成功后保留单号，双击不重提；成功但回读失败只能重新 GET。网络/5xx 结果不明时本窗口锁定提交并要求核对台账；尚无服务端持久幂等键，不能宣称任意刷新/重开或数据库重启下 exactly-once。
- 首次新专项 `1790406054625-caff2611` 暴露列表接口返回嵌套 customer.id、前端却仅读 customerId，导致续发入口消失；保留失败报告和截图。补齐映射回退，同时让权限检查等待真实订单行挂载，避免“无按钮”其实只是虚拟表格未渲染的假通过。
- 修正后 `output/round2/1790406486185-4393081f/enterprise-round2-v1.json` 通过：真实浏览器选择 60 kg 原订单行，61 kg 禁止提交，错误批号 404 后修正，双击只落一张 pending 单且库存仍 60；真实发货按钮出库后，API 双实例签收与结案回读通过。已查看 390px 弹窗和桌面成功截图；移动端本轮仅布局验收，不称完整移动写入链。
- UI 回归 `output/round2/1790406567060-1f96fbca/output/ui-ux-audit/20260926070950209-6096-99eoem/report.json` 为 5 路由 / 40 视口 / 124 状态与截图，0 错误、3 个 CRM 手机任务卡警告。前端 22 项、第二轮裁决 14 项与 TypeScript/968 文件乱码门禁通过。
- 仍未完成：结构化待补责任人、预计交期、关联补采购/生产、替代与取消退款闭环；等价换物的负差额、部分履行及完整浏览器链；原 37 项中未执行的 27 项。上述新增入口不占用或偷换这些义务。

### 续发增量合并验收与旧发货夹具修复

- `output/round2/1790406768187-1cfe4705/enterprise-round2-v1.json`：真实续发专项再次通过。浏览器对创建成功后的首个订单 GET 注入一次 503，保留真实已创建单号、禁止重提，点击“只重试读取”恢复；数据库仍只有原 40 kg 与补发 60 kg 两张单。此为浏览器读回故障注入，不是数据库重启或不确定提交验收。
- 原始第二轮 `output/round2/1790406951547-d7728302/enterprise-round2-v1.json` 仍为 10 通过 / 0 失败 / 27 未执行 / 1 条完整链，非零退出保留。
- 云端原始日志 `output/review/round2-job-108356998435.log` 第 2798 行与第 3603 行确认首次 human workflow 和后续停 MinIO 主节点时均为 `expected linked order delivered, got confirmed`。搜索从节点接管本身已通过，但整个降级切换门禁仍失败，不改写旧结论。
- 仅修发货审计夹具：先 GET 实际订单详情，唯一匹配物料 ID 与规范单位，读取持久化 orderItemId，拒绝缺行、歧义、跨单位和超行数量；提交后验证返回行关联。不迁移旧业务数据、不放松缺证据门禁。新增 3 组反例契约，裁决测试由 14 增至 **17/17 通过**。
- 主控独立原始发货回归 `output/round2/1790407017299-64fd1a42/output/playwright/shipping-audit-report-v1.json` 通过：实际订单行 ID 1 绑定发货行，订单与发货均 delivered；真实浏览器 OCR 建单、出库、POD 上传及签收回读完成，库存/批次/成本数量均 12 kg、成本余额 120、唯一出库成本 -120。已查看最终签收截图 12/12、剩余 0。
- 完整前端单元与全部浏览器契约组合通过；独立行级 helper 7/7 由主控复跑通过。图谱更新至 11541 节点 / 19377 边，生成物不提交。新包仍须推送后的 PostgreSQL 企业云验证，不能以这些 SQLite 结果覆盖旧云失败。
