# 货抵负差额闭环缺口（2026-09-26）

状态：已复现、未修复。正式 37 项验收中的 `barter-negative-cash-adjustment` 仍不能通过。

## 实际观察

- 受测业务源码 `bf6df697038a5c911e26010abdde95b3b4f77402`，本地全新 SQLite 数据库，两个应用实例、真实销售/审核/财务身份。
- 销售创建订单与 `cash_refund` 货抵协议：我方 10 kg × 20 = 200，对方 20 kg × 12.5 = 250；审核后财务过账 200。
- 两实例读回协议均为 `completed`，执行抵扣 200、剩余抵扣 0；批次 `posted`、`cashDifference=-50`；订单已收 200。收回库存的成本台账为 250。
- 财务实际页面显示协议“已完成”，批次台账显示 200 / 250 / -50 / 已过账 200，仅有“冲销”动作。
- 隔离库全部 `AdjustmentRecord` 在过账前后均为 0，按订单/客户查无关联调整记录、按订单查无 `ReceivableAdjustment`。负差额仍在批次字段及付款元数据中，没有被删除；但未形成独立退款/应付责任的处理状态和凭证链。
- 本结果不证明库存数量或成本漂移，也不代表真的支付了 50。它证明当前“抵扣完成”不能等同“差额责任已处理”。

## 证据与探索范围

- `output/round2/1790429201203-c2f2b483/enterprise-round2-v1.json` 保留上述 API/数据库证据及财务台账文本；`negative-ledger.png` 已人工查看，-50 清楚可见。协议截图截到了加载遮罩，不能据其宣称视觉验收完成，另行补测。
- 补齐等待加载遮罩消失后，`output/round2/1790429311787-0c809c2d/enterprise-round2-v1.json` 再次在真实缺口断言失败；两张截图均已人工查看，协议“已完成 / 200 / 0”与台账“差额 -50 / 已过账 200”同时成立，无加载遮罩。未修改或覆盖旧失败证据。
- 更早 `1790429097597-29df302d` 的探针把 tab 写成 button，失败于选择器，不作业务失败的唯一证据。
- 探索脚本为 `output/review/negative-barter-{runner,audit,probe}.cjs`，复用既有隔离环境与鉴权夹具，不改生产源码。会创建一个未过账的独立夹具批次，再对新协议批次实际过账；未过账夹具不参与库存或核销。
- 该专项只执行负差额一项，因此报告中的 0 通过 / 1 失败 / 36 未执行不是正式整轮倒退；最近正式本地整轮仍是 11 / 0 / 26。探索脚本不具备给完整负差额义务授予通过的能力。

## 下一包修复边界

1. **独立差额责任**：按过账批次记录方向、对方、币种、初始金额、已处理额/待处理额、责任人、凭证关联和状态。不得把多收货值伪装成销售现金回款，或通过修改订单 paidAmount 来平掉应付差额。
2. **原子、唯一、可回读**：责任记录与货抵过账同一事务；同批次只能生成一笔原始责任，重放不可重复。金额来自被冻结的估值，而非前端任意覆盖。
3. **允许不同处置方案**：退款、结转可用余额或经审批的调整须有明确业务凭证；登记责任不等于实际付款。未经授权不自动向外支付资金。
4. **拆开完成状态**：抵扣数量/金额完成与差额处置完成分别呈现。不能仅因 remainingOffsetAmount=0 就向使用者暗示全部财务义务已结清。
5. **反冲与并发**：未处置的责任可随原批次原子撤销；已退款/已使用余额的批次须阻止直接反冲，或走完整补偿凭证链，不能删除历史。测试双财务同时处置、重复请求、处置与反冲竞争。
6. **验收**：覆盖负 50 的生成/处理/回读、重复处理不增加金额、不同币种不混算、反冲恢复、财务与销售权限边界、浏览器实际操作、双实例 API 和数据库对账；迁移同时覆盖 SQLite 与 PostgreSQL。

非目标：此文不是已实现承诺，不通过负差额义务，不改动原验收目录，也不以文案调整替代财务模型修复。
