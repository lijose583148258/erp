interface:
  display_name: "ERP Encoding Triage"
  short_description: "Garbled text and wrong entry"
  default_prompt: "Use $erp-encoding-triage-erp to execute this role with strict timeboxes, evidence capture, and minimal-risk fixes."

policy:
  allow_implicit_invocation: true

