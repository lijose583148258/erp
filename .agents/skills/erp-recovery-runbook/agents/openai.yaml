interface:
  display_name: "ERP Recovery Runbook"
  short_description: "Restart, rebuild, rollback"
  default_prompt: "Use $erp-recovery-runbook to execute this role with strict timeboxes, evidence capture, and minimal-risk fixes."

policy:
  allow_implicit_invocation: true

